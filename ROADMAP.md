# نقشهٔ راه KIMIA

هدف: **قابلیت زیاد، بدون کدنویسی.** هر کاری باید از رابط کاربری شدنی باشد؛ جایی که کد لازم است، کدِ آسان باشد نه C++.

هیچ محدودیتی به‌خاطر ساخت روی گوشی پذیرفته نیست؛ رابط کاربری و قابلیت‌ها باید هم‌تراز موتورهای مطرح باشند — روی لمس و دستهٔ بازی.

---

## وضعیت فعلی

### ساخته‌شده

- صحنه، موجودیت، ذخیره/بارگذاری: SceneIO v1 و v2 (شناسهٔ پایدار موجودیت)،
  نمایهٔ نام، کامپوننت دیالوگ و هدف دوربین — [سند Scene](Documentation/Scene.md)
- فیزیک: کالایدر، raycast، کنترل‌گر چندکاراکتری، باد، آب‌وهوا
- رندر: GL/GLES3/WebGL2/D3D11 + رسترایزر نرم‌افزاری تضمینی، سایه، PBR، تون‌مپ
- دارایی: OBJ/FBX با متریال/تکسچر، PNG/JPG، WAV/MP3/OGG/FLAC
- اسکلت، retarget، انیمیشن از FBX، آدمک استخوانی
- منطق بصری (رویداد ← شرط ← کنش)، متغیرها
- سازندهٔ UI بازی (HUD: label/bar/box/button)
- سیستم ورودی (کیبورد/دسته/لمس با ناحیهٔ گرد)
- ذرات (جرقه، دود، انفجار)
- Blueprint و صحنه‌های چندگانه
- انتشار بازی به یک فایل `game.kimia`
- ردیاب پرتو آفلاین (PBR + GI + BVH، خروجی PNG دترمینیستیک)
- KIMIA Editor (WebWorkbench) به سبک Unity
- APK بومی اندروید (GLES3) و EXE خودکفای ویندوز (D3D11)

### زیرساخت کیفیت (زنده از 0.29.1)

- پنج job در GitHub Actions: بیلد لینوکس با `-Werror`، کل CTest، همان
  تست‌ها زیر ASan+UBSan و زیر TSan، smoke بیلد MSVC، و APK/EXE.
- `bash Tools/run_tests.sh [--sanitize|--tsan]` و target `check` — یک
  دستور برای بیلد + کل تست‌ها، همان چیزی که CI اجرا می‌کند.
- سند [CI](Documentation/CI.md): گزینه‌ها، معنی هر job و استثناهای مستند.

### در حال تکمیل

- فاز ۳ (صحنه/موجودیت): هویت و نمایهٔ نام ✅، فایل نسخه‌دار v2 ✅، دیالوگ ✅،
  هدف دوربین ✅، **موتور کاراکتر (v3) با سیم‌کشی واقعی به حرکت بازیکن** ✅
  ([سند Scene](Documentation/Scene.md)،
  [سند موتور کاراکتر](Documentation/CharacterMotor.md))؛ باقی‌مانده:
  `Ability` (فاز ۶)، `ReplayMarker` (فاز ۷)
- فاز ۲: شکستن `World.cpp` ✅ (۱۴ فایل، هر کدام یک حوزه، بدون تغییر رفتار —
  [Architecture](Documentation/Architecture.md))؛ `WorldRuntime`,
  `CinematicCamera`, `ReplaySystem` با فاز ۷ می‌آیند
- هم‌سازی کامل مسیر رندر D3D11 با GL (همان PBR/تون‌مپ)
- ~~راستی‌آزمایی واقعی بیلد WebAssembly/Emscripten~~ — job `wasm-smoke` در
  CI سبز شد (اجرای 35596687112) و دو باگ واقعی پیدا کرد که یکی‌شان
  `Examples/WebGLApp.cpp` هرگز کامپایل‌نشده بود
- اجرای تست‌ها روی ویندوز: `Tests/src/WebTests.cpp` سوکت POSIX می‌خواهد،
  پس job ویندوز فعلاً فقط smoke بیلد است
- ایمن‌سازی کامل تنظیم‌کننده‌های سرور WebViewer برای تغییر پس از شروع
  سرو کردن ترافیک (فاز ۱۰)

---

## برنامهٔ آینده

### فیزیک (فاز ۴، شروع‌شده)

- ✅ **فاز پهن**: جاروب و هرس روی X، با مسیر خطی به‌عنوان مرجع تست‌شده
  (بیت‌یکسان). ۱۰۰ جسم: ۵٫۶۶ → ۰٫۱۰ ms/گام؛ ۱۰۰۰ جسم: ۷۱۴ → ۱٫۷ ms/گام.
  [سند فیزیک](Documentation/Physics.md) · [کارایی](Documentation/Perf.md) ·
  `Tools/src/kimia_bench_physics.cpp`
- ✅ **پله در کنترل‌گر کاراکتر**: `kCharacterStepHeight` ۰٫۳۵ — جدول/لبهٔ جدول
  بالا می‌رود، دیوار بالاتر از آن نه؛ `stepHeight = 0` رفتار قبلی.
  تشخیص زمین با قاعدهٔ پا و قاعدهٔ «نفوذِ ساختهٔ حرکت». ۶ تست.
- ✅ **مرزهای زمین**: تخته‌ها توپ را برمی‌گردانند
  (`kWorldBoardRestitution` ۰٫۵۵) و توپِ روی خط پایان، گل‌کیک می‌گیرد
  (`Stoppage::GoalKick`)؛ کلیرِ دفاع روی هر دو محور به داخل زمین خم می‌شود.
  دلیل: توپی که روی خط پارک شود **بازی‌شدنی نیست** — پس‌زدنش بازیکنی می‌خواهد
  که بین توپ و تخته باشد و زمین زودتر تمام می‌شود. ۴ تست.
- ✅ **ماده‌های زمین**: چمن (خنثی)، آسفالت، بتن، فلز، چوب، لاستیک، شن —
  دو ضریب (گیر/برگشت) در `Physics.h`، جدول و نام‌ها در `GameProfile.h`؛
  کلید پروفایل `surface`، انتخاب محیط در ویرایشگر، و `/api/surface`.
  خیسی روی ماده ضرب می‌شود. ۹ تست.
- ✅ **CCD توپ**: کره‌ای که در یک گام بیش از نصف شعاعش جابه‌جا شود، جاروب
  می‌شود (جعبه‌های ساکن/دینامیک + صفحهٔ زمین)، تا ۳ برخورد در یک گام.
  زیر آستانه، مسیر گسستهٔ قبلی بیت‌به‌بیت. `setCcdEnabled` برای مقایسه.
  هزینه: داخل نویز (۵٫۲۷ در برابر ۵٫۳۳ ms/گام در صحنهٔ ۱۰۰۰ جسمی). ۶ تست.
- ⬜ بقیهٔ فاز ۴: اصطکاک غلتشی/سرشی جدا، شیب مورب (جعبهٔ چرخیده)،
  مرکز جرم/اینرسی برای اجسام صلب، خواب (sleeping)

### هوش مصنوعی (فاز ۶)

- ✅ **تصمیم امتیازدهی‌شدهٔ دارندهٔ توپ**: شوت / پاس / بردن، با تابع خالصِ وضعیت
  (بدون RNG) — پاس به یارِ آزادِ جلو، با پیش‌بینی محل آیندهٔ یار. بازی ۳ دقیقه‌ای:
  ۳۷ پاس، ۴۰ شوت، ۲۶۴ چالش، گل.
- ✅ **رویدادهای لمسی**: `Pass`/`Kick`/`Tackle` با فاصلهٔ ۰٫۶ ثانیه‌ای، نه هر فریم.
- ✅ **ابزار دیباگ** `GET /api/ai`: نقش، نقطهٔ هدف، عمل انتخابی و همهٔ امتیازها.
  [سند هوش مصنوعی](Documentation/Ai.md)
- ✅ **دروازه‌بان و خط دفاعی (0.30)**: keeper پرواز توپ را می‌خواند، داخل قابِ
  واقعی دروازه می‌ماند، می‌گیرد و با رویداد `Save` پاک می‌کند؛ ساپورت‌ها با توپِ
  حریف خط دفاعی می‌بندند. مسابقهٔ ۳ دقیقه‌ای: ۲–۱ با ۶۰ سیو (پیش‌تر ۱–۱۱)؛
  تست عددی بازه و تست قطعیت بیت‌به‌بیت.
- ⬜ بقیهٔ فاز ۶: نقش‌های تخصصی (PressingDefender/Winger/Playmaker/StreetTrickster)،
  صفات seed-محور هر بازیکن، تاکتیک تیمی

### انیمیشن (فاز ۵)

- ✅ **گیت از سرعت واقعی (0.30)**: حالت‌های Idle/Walk/Run/Sprint/Stopping از
  جابه‌جایی تحقق‌یافتهٔ بدن (نه ورودی)، با رمپ blend ۰٫۲۵ ثانیه‌ای روی هر گذار؛
  در `/api/ai` قابل دیدن. تست: مجسمهٔ کنار دیوار Idle است، AI بدون ورودی Run.
- ⬜ وصل کردن clip اسکلتی به گیت، foot IK، contact زمان ضربه، hit reaction

### منطق و گیم‌پلی

- [ ] اسکریپت آسان (زبان کوچک تک‌خطی برای جاهایی که بلوک کم می‌آورد)
- [ ] حالت بازی کامل: امتیاز، جان، برد/باخت، ذخیرهٔ پیشرفت (checkpoint)
- [ ] ماشین حالت برای شخصیت/گیم‌پلی (ایست ← راه ← دو ← پرش)

### ویرایشگر

- [ ] چندانتخاب (multi-select) و تکثیر
- [ ] undo/redo
- [ ] دوربین آزاد در ویرایشگر
- [ ] زمین قابل‌ویرایش با قلم (ارتفاع‌نگار)
- [ ] خط زمان (keyframe روی هر خاصیت)
- [ ] نورپردازی تعاملی (جهت‌دار/نقطه‌ای، سایه، مه)

### رندر

- [ ] تکمیل و هم‌سان‌سازی پشتیبانی PBR در D3D11
- [ ] آمیختن (blending) انیمیشن‌ها

### چندنفره

- [ ] فیزیک شبکه و چندنفره (پیش‌نیاز بتل گراند کامل)

---

## قواعد پابرجا

- موتور محتوای بازی نمی‌سازد؛ ابزار می‌دهد.
- هر قابلیت با تست مقدار-واقعی و صفر اخطار در همهٔ بیلدها.
- هیچ قابلیتی «تمام‌شده» نیست تا وقتی از رابط کاربری قابل استفاده نباشد.
