# دفترچهٔ نسخه‌های موتور بازی‌سازی کیمیا

هر بار که موتور به نسخهٔ جدیدی می‌رسد، یک بخش به **بالای** این فایل اضافه
می‌شود: نسخهٔ موتور، زمان توسعه، تاریخ امروز و آنچه اضافه شد. این یادگار
ماندگار پروژه است — هیچ بخشی بعداً حذف یا بازنویسی نمی‌شود.

**قاعدهٔ شماره‌گذاری (از `0.1.0`):** نسخه = `اصلی.فرعی.وصله` و فقط با
**اندازهٔ تغییر** جلو می‌رود، نه با تاریخ:

| تغییر | شماره | مثال |
| --- | --- | --- |
| چیز کوچک یا رفع باگ | وصله +۱ | `0.1.0` → `0.1.1` |
| یک مرحله یا تغییر بزرگ | فرعی +۱ و وصله = ۰ | `0.1.1` → `0.2.0` |
| اصلی | فقط وقتی کاربر اعلام کند (انتشار یک بازی) | `0.9.4` → `1.0.0` |

همین رشته در `Engine/Core/include/kimia/Version.h` (`kimia::kEngineVersion`)
و در `project(KIMIA VERSION …)` فایل `CMakeLists.txt` نوشته می‌شود؛ یک تست
برابری آن‌ها و این را که **تازه‌ترین بخش همین دفترچه همان نسخه باشد** نگه
می‌دارد. نسخه‌های پیش از `0.1.0` تاریخ شمسی انتشار بودند (`1405.06.05` تا
`1405.06.14.3`)؛ آن بخش‌ها همان‌طور که نوشته شده‌اند می‌مانند.

**زمان توسعه** از مُهر زمانی کامیت‌های Git محاسبه می‌شود (فاصلهٔ اولین تا
آخرین کامیت هر نسخه، به وقت تهران)؛ عدد تقریبی و صادقانه است، نه تخمین.

**تقسیم کار:** بازی را کاربر می‌سازد و در حالت‌های مختلف آزمایش می‌کند؛ موتور
فقط **قابلیت** اضافه می‌کند (ابزار، جسم، قانون، کلید پروفایل) — نه زمین، نه
محتوا، نه تنظیم نهایی.

---

## نسخهٔ 0.31.0 — پاها از گیت پیروی می‌کنند: کلیپ اسکلتی، از حرکت واقعی

**تاریخ:** ۱۴۰۴/۰۷/۰۴ (2026-09-25)

0.30 گیت را ساخت (بدن چه می‌کند) ولی کسی آن را بازی نمی‌کرد: بازیکن‌ها یا
مجسمه‌های مفصل‌دار رویه‌ای بودند یا مدلِ بی‌حرکت. حالا حلقه بسته است —
بازیکن مدلِ پوست‌دار می‌پوشد و کلیپِ همان حالتی را می‌خواند که واقعاً در آن است.

- **کلیپ روی کاراکتر، از گیت:** کاراکترهای مسابقه بدن فیزیکی‌اند نه
  موجودیت، پس انیمیشن‌شان روی همان موجودیتِ نقشی می‌نشیند که رندرر از قبل
  برای ریگ می‌خواند: `Player` برای انسان، `Keeper` اگر دنیا داشته باشد، و
  `Squad` برای بقیه. کافی است آن موجودیت مدل اسکلت‌دار داشته باشد و
  کامپوننت‌های Animation با **trigger** برابر نام حالت گیت (`idle`, `walk`,
  `run`, `sprint`, `stopping`).
- **زنجیرهٔ جانشین:** حالتی که کلیپ ندارد، از حالت پایین‌تر قرض می‌گیرد
  (sprint → run → walk → idle، و stopping → walk). فایل نبودن نباید بازیکنِ
  در حال دویدن را در ژست T میخکوب کند.
- **سرعت پخش از سرعت واقعی:** نرخ پخش = سرعت تحقق‌یافته ÷ سرعت پروفایل،
  محدود به ۰٫۳۵ تا ۱٫۶؛ Idle و Stopping نرخ مؤلف را نگه می‌دارند. تغییر
  حالت کلیپ را با همان ۰٫۲۵ ثانیهٔ رمپ گیت کراس‌فید می‌کند، و تغییر فقط
  سرعت، کلیپ را ری‌استارت نمی‌کند (`Animator::setPlaybackSpeed`).
- **رندر و اشکال‌زدایی:** فریم، مدلِ پوزشدهٔ کاراکتر را می‌کشد
  (`posedCharacterMesh`) و در گزارش، `posedCharacters` می‌شمارد؛
  `/api/ai` حالا `clip` و `clipSpeed` هر بازیکن را هم می‌دهد. دنیای بدون
  asset کاراکتر **هیچ تغییری نمی‌بیند** — همان فیگورهای قبلی.
- **ابزار تست:** `Tools/make_skinned_fbx.py` حالا چند کلیپ در یک FBX
  می‌نویسد (`--clips "Idle:1.0:8,Stride:0.5:60"`) و دارایی آزمایشی
  `Tests/assets/skinned_bar_gait.fbx` از همان ساخته شده.
- ۴ تست تازه (دو در دنیا، یکی برای سازگاری عقب‌رو، یکی در فریم) → ۵۷۴ تست.

## نسخهٔ 0.30.0 — دروازه‌بان، دروازه‌بان شد:سیو، خط دفاعی، و گیت از سرعت واقعی

**تاریخ:** ۱۴۰۴/۰۷/۰۳ (2026-09-24)

مسابقهٔ ۳ دقیقه‌ای بدون انسان تا دیروز ۱–۱۱ تمام می‌شد (و در یک فریمِ
نمونه‌برداری‌شده ۰–۱۷): دروازه‌بان فقط به جایِ **فعلی** توپ نگاه می‌کرد و
داخل یک ثابت سراسری قدم می‌زد، نه داخل قابِ واقعیِ دروازهٔ همان صحنه؛ و
خط دفاعی وقتی توپ مال حریف بود همان شکلِ حمله‌ای را نگه می‌داشت دور توپی
که مال خودش نبود.

- **دروازه‌بان پرواز توپ را می‌خواند** (تا ۰٫۵ ثانیه پیش‌بینی خطی، محدود و
  قطعی) و داخل `aiGoalMouthHalf` همان صحنه می‌ماند؛ وقتی توپ به او می‌رسد
  **می‌گیرد و پاک می‌کند** — به پهنا و به نیمهٔ حریف — و رویداد تازهٔ `Save`
  ثبت می‌شود.
- **خط دفاعی:** با توپِ مال حریف، ساپورت‌ها روی خطی در ۰٫۳۵ فاصلهٔ دروازهٔ
  خودی تا توپ می‌ایستند و به سمت توپ متمایل می‌شوند.
- همان مسابقه حالا **۲–۱ با ۶۰سیو و ۶۷ پاس** است؛ و یک تست عددی بازه را
  نگه می‌دارد: بین ۱ تا ۶ گل در ۳ دقیقه، دست‌کم ۲۰سیو.
- **قطعیت:** همان مسابقه دو بار، بیت‌به‌بیت یکسان — امتیاز،سیوها و جای
  نهایی توپ (پایهٔ ری‌پلی فاز ۷).
- **فاز ۵ از نقطهٔ صادق شروع شد:** حالت گیت (Idle/Walk/Run/Sprint/Stopping)
  از سرعتِ **تحقق‌یافته** (جابه‌جایی هر فریم)، نه از ورودی؛ رمپ blend روی هر
  گذار. بازیکن گیرکرده به دیوار با ورودی کامل، Idle است.
- یک باگ واقعی که همان تست گرفتش: در حالت مسابقه به بدن بازیکن انسان هیچ
  clamp مرزی اعمال نمی‌شد (با ورودی نگه‌داشته روی زمین ۸ متری به z=25
  رسید!). حالا مثل بقیهٔ یارها clamp می‌شود.
- ۵ تست تازه → ۵۷۰ تست.

## نسخهٔ 0.29.1 — گیت کیفیت: CI زنده، سنیتایزر سبز، و یک data race واقعی که پیدا شد

**تاریخ:** ۱۴۰۴/۰۶/۳۰ (2026-09-21)

تا امروز «۴۷۵ تست سبز» یک ادعا در README بود. حالا یک دستور است و یک
job در CI: `bash Tools/run_tests.sh` (و `--sanitize` / `--tsan`) همان
کاری را می‌کند که GitHub Actions، پس اجرای محلی و اجرای CI از هم جدا
نمی‌افتند. سه workflow غیرفعال (اندروید و ویندوز) دوباره فعال شدند و یک
`ci.yml` اضافه شد: بیلد لینوکس با `-Werror`، کل CTest، همان تست‌ها زیر
AddressSanitizer+UBSan و ThreadSanitizer، یک smoke بیلد MSVC روی ویندوز،
و یک job وِب‌اسمبلی که تا اولین اجرای سبز، «advisory» و زرد می‌ماند.

سنیتایزرها سه چیز واقعی پیدا کردند و هر سه بسته شد:

- **`ufbx` زیر UBSan می‌مرد.** برای FBX، inflate سریع ufbx عمداً روی
  شیفت چرخشی x86 حساب می‌کند که در C undefined است. خود ufbx کلید
  `UFBX_UBSAN` را دارد؛ clang خودش تشخیص می‌دهد، ولی GCC برای
  `-fsanitize=undefined` هیچ ماکرویی تعریف نمی‌کند، پس صریح پاس داده شد.
- **`stb_image_write`** در نویسندهٔ JPEG بیت علامت را با شیفت چپ پر
  می‌کند. آن فایل به یک TU جدا رفت تا تنها تخفیفِ سنیتایزر روی کد
  vendored باشد، نه روی کد ما.
- **`web::Server::stop()` یک data race واقعی داشت** (`listenFd` بین نخ
  پذیرش و `stop()`). ضمن رفع، دو مشکل همسایه هم درست شد: `close()` قبل
  از `join()` صدا می‌شد — که می‌تواند شمارهٔ توصیفگر را به سوکت دیگری در
  همان پروسه بدهد — و handlerهای detached می‌توانستند بعد از آزاد شدن
  state سرور به حافظهٔ آزادشده دست بزنند؛ اکنون `Server::Impl` با
  `shared_ptr` زنده نگه داشته می‌شود.

کنار آن: سوئیچ `KIMIA_SANITIZE` و پریست‌های `linux-release` /
`linux-sanitizers` / `linux-tsan`، target `check`، و اصلاح مسیرهای
اشتباه مستندات (نام ریپو و شاخه در README/GettingStarted/Termux/PS4) و
سند تازهٔ `Documentation/CI.md`.

**تأیید زنده:** `bash Tools/run_tests.sh --clean` → ۴۷۵/۴۷۵ سبز با صفر
اخطار؛ `--sanitize` → ۴۷۵/۴۷۵ سبز و «چیزی پیدا نشد»؛ `--tsan` → بعد
از رفع مسابقه، ۴۷۵/۴۷۵ سبز و بدون گزارش race.

## نسخهٔ 0.29.0 — دوئل خیابونی: فوتبال یک‌به‌یک با وقت و امتیاز

**تاریخ:** ۱۴۰۴/۰۶/۱۶ (2026-09-07)

یک بازیکن تنها با ساعت مسابقه حالا یک «دوئل» است: تو (تیم ۱) به‌علاوهٔ
یک حریف کامپیوتری (تیم ۲) که دنبال توپ می‌آید و به دروازه‌ات حمله
می‌کند. گل، سوت پایان، شروع دوباره از وسط زمین و جدول امتیاز همانِ
مسابقه‌های چندنفره‌اند. گلف دست‌نخورده ماند: یک بازیکنِ بدون ساعت
هنوز فقط تفریح است، نه مسابقه.

دنیای `street_kids` هم برای دوئل بازسازی شد: کوچه حالا در راستای Z
(۱۶ متر) با دو دروازهٔ واقعی — یکی توری در انتهای دور و یکی آجری با
تیرک چوبی در انتهای نزدیک — سه دقیقه وقت، توپ فانتزی و حریف با مهارت
۰٫۶. بچه‌های تماشاچی رفتند کنار خط.

**تأیید زنده:** فایل واقعی دنیا لود شد، بازی شروع شد، توپ با شوت واقعی
به تور دروازهٔ دور رفت (`MA 1 - 0 ANHA`) و بعد از شادی گل، بازی از وسط
زمین با هر دو بازیکن از سر گرفته شد؛ ساعت هم کم می‌شد.

## نسخهٔ 0.28.2 — دنیای کوچه بازی‌کردنی شد

**تاریخ:** ۱۴۰۴/۰۶/۱۶ (2026-09-07)

دنیای `street_kids` بازیکن نداشت، پس دکمه‌های حرکت بی‌اثر بودند و حس
«دکمه‌ها خراب‌اند» می‌دادند. حالا علی (کلاه و پیراهن راه‌راه) به‌عنوان
`Player` وسط کوچه است و با جهت‌ها حرکت می‌کند و توپ را هل می‌دهد.

همراهش رندر بازیکن مدلی درست شد: کنترلر کاراکتر «مرکز» بدن را
می‌دهد ولی مدل روی پاهایش می‌ایستد، پس نیم‌متر شناور می‌شد؛ حالا به
اندازهٔ نیم‌قد پایین می‌آید. مکعبِ سرِ اضافه هم وقتی مدلِ تمام‌قد
هست کشیده نمی‌شود.

**تأیید زنده:** دنیا باز شد، با نگه داشتن «بالا» بازیکن به‌سمت توپ
رفت و آن را جلو برد؛ فریم قبل و بعد از `/frame.png` گرفته شد.

## نسخهٔ 0.28.1 — رندر چندمتریاله و پک فوتبال خیابونی

**تاریخ:** ۱۴۰۴/۰۶/۱۶ (2026-09-07)

مدل‌ها تا دیروز تک‌رنگ کشیده می‌شدند (رنگ آبجکت) و رنگ‌های MTL فایل
نادیده گرفته می‌شد. حالا هر فایل مدلی که متریال داشته باشد، تکه‌تکه با
رنگ خودش کشیده می‌شود (`assetFor` + `modelTints`، با کش مثل
`skinnedFor`)؛ رنگ آبجکت هم به‌جای جایگزینی، tint می‌کند، پس paint
کردن هنوز کار می‌کند. مشِ پوزگرفته و فایل بی‌متریال مثل قبل یک‌تکه‌اند.

همراهش پک `assets/street` آمد: ۵ وسیله (دروازه کوچک، مخروط، لاستیک،
آجر، نیمکت) و ۳ بچهٔ کوچه (علی با کلاه و پیراهن راه‌راه، رضای
پابرهنه، حسن قدبلند) — همه low-poly و جدا از هم، با اسکریپت سازنده در
`Tools/make_street_models.py` و دنیای نمونهٔ `Worlds/street_kids.kimia`.

**تأیید زنده:** دنیای کوچه روی سرور واقعی بالا آمد؛ بچه‌ها با رنگ‌های
جدا، دروازه، آجرها و درب آبی سر جایشان بودند و فریم از `/frame.png`
گرفته شد.

## نسخهٔ 0.28.0 — فایل انیمیشنِ بدون مش و آدمک استخوانی

**تاریخ:** ۱۴۰۴/۰۶/۱۶ (2026-09-07)

پک `assets/animations` (۳۹ فایل فوتبالی Mixamo) فقط اسکلت و حرکت دارد،
بدون هیچ مش — و لودر تا دیروز چنین فایلی را رد می‌کرد. حالا
`loadFBXSkinned` از فایل بدون مش هم اسکلت و کلیپ را می‌خواند، ایمپورت آن
را به‌عنوان مدل می‌پذیرد، و به‌جای مش، **آدمک استخوانی زنده** می‌کشد:
یک جعبهٔ کشیده برای هر استخوان و یک مکعب روی هر مفصل، با ضخامت خودکار
(۲٪ قد خود فیگور) تا واحد فایل (میکسمو سانتی‌متر می‌نویسد) مهم نباشد.

نام کلیپ هم از نام فایل می‌آید، چون هر ۳۹ فایل اسم استک‌شان یکی است
(`mixamo.com`) وگرنه همهٔ کلیپ‌ها هم‌نام می‌شدند.

**تأیید زنده:** هر ۳۹ فایل باز شد (۶۵ تا ۶۹ استخوان، صفر خطا)؛ یک دنیای
نمایشی با سه آدمک (دریبل، سیو، پیروزی) بالا آمد و فریم‌به‌فریم حرکتشان
از `/frame.png` گرفته شد.

## نسخهٔ 0.27.1 — مدیریت فایل‌های asset در پنل Project

**تاریخ:** ۱۴۰۴/۰۶/۱۶ (2026-09-07)

پنل Project حالا یک مدیر فایل کوچک است: هر فایل باز می‌شود (مدل→صحنه،
عکس→روی جسم)، درجا تغییرنام می‌دهد، با تأیید حذف می‌شود، و دکمهٔ Upload
فایل را از کامپیوتر/گوشی توی پوشهٔ assets می‌ریزد (POST با بدنهٔ خام،
سقف ۳۲ مگ). مسیرهای `/api/asset/rename` و `/api/asset/delete` و هندلر
آپلود به سرور اضافه شد.

**امنیت:** همهٔ نام‌ها باید لخت باشند (`..` و اسلش رد می‌شود)، آپلود روی
نام تکراری نمی‌نشیند، و فایل ناقص پاک می‌شود نه اینکه مدل نصفه بماند.
بازیِ منتشرشده نه API و نه آپلود دارد، مثل قبل.

**تأیید زنده:** سرور واقعی بالا آمد؛ آپلود/تغییرنام/حذف/رد بازنویسی/رد
`..`/خطای 405 روی GET، همه با curl چک شدند.

**تست:** ۳ تست جدید (۴۴۳ از ۴۴۳) و صفر هشدار با فلگ‌های strict.

## نسخهٔ 0.27.0 — ویرایشگر به سبک Unity و انیمیشن از اسکلت خود FBX

**تاریخ:** ۱۴۰۴/۰۶/۱۶ (2026-09-07)

صفحهٔ ویرایشگر از نو نوشته شد: چیدمان Unity (از چپ Hierarchy، وسط
Inspector، راست و بزرگ Game) با نوار ابزار بالا و Project/Console
پایین — هر خطِ کد و استایل از صفر، فقط چیدمان و اصطلاح‌ها از Unity. هر
۵۴ مسیر قدیمی `/api/*` سر جایشان‌اند و کار می‌کنند؛ ۱۳ مسیر جدید
(`object/*` و `transport/*`) کنارشان نشست.

**۱. ابزارهای ترنسفورم.** Select/Move/Rotate/Scale با کلیک یا Q/W/E/R؛
درگ در نمای Game با ابزار فعال کار می‌کند؛ اسنپ برای حرکت (گرید) و
چرخش (گام ۱۵ درجه).

**۲. Play/Pause/Step.** دکمه‌های نوار ابزار، و در برنامه Return شروع،
Tab توقف/ادامه، Backspace یک فریم. Pause فقط شبیه‌سازی را فریز می‌کند،
نه منوها را.

**۳. Inspector عددی.** سه‌تایی Position/Rotation/Scale با عدد (چرخش به
درجه)، Duplicate (دکمه و Ctrl+D)، Rename، و ساخت جسم از نوار ابزار
(Cube/Sphere/Plane و Block/Wall/Goal/Crate/Hole/Player/Ball).

**۴. انیمیشن واقعی.** کلیپ از اسکلت خود فایل FBX پخش می‌شود نه از
تایمر: کش اسکلت برای هر فایل، طول واقعی کلیپ از فایل، و `posedMesh`
که رندر هر فریم را می‌پوشاند. کنترلی که به یک فایل وصل است، همهٔ
جسم‌هایی را که از آن فایل‌اند می‌رقصاند.

**۵. چرخش در صحنه.** موجودیت‌ها کواترنیون چرخش دارند (`rot` اختیاری در
فایل صحنه، سازگار با قبل) و رندر آن را اعمال می‌کند. فیزیک عمداً
محور-هم‌راستا مانده: دیوار چرخیده هنوز مثل همان جعبه برخورد می‌کند —
محدودیتِ مستند، نه سکوت.

**تست:** ۲۰ تست جدید (۴۴۰ از ۴۴۰) و صفر هشدار با فلگ‌های strict.

## نسخهٔ 0.26.0 — اسکن فایل، سیستم ورودی، و بافت از فایل‌منیجر

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

چهار چیزی که کاربر خواست، همه در یک نسخه.

**۱. اسکن فایل‌ها.** فایل‌هایت را در `assets` می‌ریزی و Workbench نشانشان
می‌دهد. دکمهٔ **Scan** داخل هر مدل را می‌خواند و **اسکلت و نام کلیپ‌ها** را
بیرون می‌کشد. فقط فایل‌های قابل‌استفاده فهرست می‌شوند، و اسکن عمیق اختیاری
است چون باز کردن هر FBX روی گوشی کند است.

**۲. سیستم ورودی.** بازی می‌گوید «پرش»؛ اینکه بازیکن روی شیشه می‌زند، Space
می‌زند یا دکمهٔ A دسته را فشار می‌دهد مسئلهٔ موتور است نه نویسندهٔ قاعده. یک
**کنترل** می‌سازی و کلید، دکمهٔ صفحه و دکمهٔ دسته را زیرش می‌گذاری.

**۳. کلیپ FBX زیر هر دکمه.** کلیپ را از فهرست کشویی انتخاب می‌کنی — همان‌ها
که Scan پیدا کرده — نه چیزی که باید از حفظ تایپ کنی. استیک حرکت هم اختیاری
است.

**۴. عکس روی جسم.** جسم را انتخاب می‌کنی، روی عکس در فهرست فایل‌ها می‌زنی،
و روی جسم می‌نشیند. فایلی که عکس نیست **رد می‌شود** نه اینکه جسم را خالی
بگذارد و آدم بماند چرا.

**دو تصمیم:**
- **دکمه‌ها گردند** چون انگشت گرد است؛ مربعِ هم‌اندازه ضربه‌های نزدیک را
  می‌دزدد.
- **کنترل یک رویداد هم هست**، پس دکمه قبل از وصل کردن کلیپ هم کار می‌کند.

**یک باگ واقعی:** اولش کلیپِ کنترل را به `fireTrigger` می‌دادم، ولی آن فقط
کامپوننت‌های انیمیشنِ دستی‌چسبانده را پیدا می‌کند. کلیپی که از یک FBX
اسکن‌شده انتخاب می‌شود کامپوننتی پشتش ندارد، پس **انتخاب کلیپ برای دکمه هیچ
کاری نمی‌کرد**. با اجرای زنده پیدا شد نه با تست.

**تأیید زنده:** ۶ فایل در پوشه ریختم، Scan اسکلت ۲ استخوانه و کلیپ `Bend` را
پیدا کرد، عنکبوت را وارد کردم و بافت شطرنجی رویش گذاشتم، دو دکمه و یک استیک
روی صفحه آمد، و هم کلید و هم دکمهٔ صفحه کلیپ را پخش کردند.

**تست:** ۱۲ تست جدید (۴۲۰ از ۴۲۰) و هر سه جهش گرفته شدند.

## نسخهٔ 0.25.0 — ذرات: جرقه، دود و انفجار

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

بدون این‌ها بازی مثل چند جعبهٔ متحرک به نظر می‌رسد.

یک افکت یک **دستور** است نه برنامه: «شصت جرقهٔ نارنجی، به بالا، که در یک
ثانیه محو می‌شوند». `count`، `life`، `speed`، `spread` (۰ = فواره، ۱ =
گلوله)، `gravity` (منفی می‌افتد، مثبت مثل دود بالا می‌رود)، اندازه و دو رنگ.

**در قواعد:** کنش تازهٔ `effect` یک افکت را روی یک جسم پخش می‌کند —
`WHEN collision Ball + Wall  DO effect explosion on Ball` — بدون کد.

**سه تصمیم:**
- **جاذبه مال هر ذره است نه سیستم.** اولین بار مشترک نوشتمش و بلافاصله
  دیدم دودِ بالارونده و جرقهٔ افتان نمی‌توانند هم‌زمان وجود داشته باشند.
- **طول عمر تصادفی است** (۷۰٪–۱۳۰٪)، وگرنه انفجار مثل خاموش شدن کلید
  ناپدید می‌شود نه محو شدن.
- **پراکندگی قطعی است** — مولد خودمان، نه `<random>` که روی ماشین‌های
  مختلف نتیجهٔ یکسان قول نمی‌دهد. تکرار یک مسابقه باید همان فیلم باشد.

**تأیید زنده:** افکتی ساختم، به کلید فاصله وصلش کردم، و ۶۰ جرقهٔ نارنجی روی
صفحه پخش شد.

**تست:** ۷ تست جدید (۴۰۸ از ۴۰۸) و هر سه جهش بار اول گرفته شدند.

یک تست اولش شکست و **اشتباه از خودم بود**: ۱٫۴ ثانیه جلو رفته بودم، در حالی
که بلندترین عمر ممکن ۱٫۳ ثانیه است — پس انتظار بازمانده داشتم بعد از اینکه
همه مرده بودند. حساب من غلط بود نه موتور.

## نسخهٔ 0.24.0 — انتشار: دادن بازی به دیگری

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

تا اینجا می‌شد بازی ساخت ولی **نمی‌شد آن را به کسی داد**.

دکمهٔ **Publish** پوشه‌ای می‌سازد با `game.kimia` (کل بازی: صحنه، فیزیک،
قواعد، متغیرها، صفحهٔ بازی)، `play.sh` (یک خط اجرا) و `README.txt`. باینری
را کنارش کپی می‌کنی و تمام.

**بازیِ منتشرشده ویرایشگر ندارد.** کسی که بازی را به او می‌دهی **بازیکن**
است؛ هر دکمهٔ ویرایشی راهی است برای خراب کردن کارت. پس:

- مستقیم وارد بازی می‌شود، نه منو
- `/bench` **۴۰۴** است و لینکش روی صفحه نیست
- **API ویرایش هم جواب نمی‌دهد** — اگر فقط صفحه را برمی‌داشتیم، پنهان‌کاری
  بود نه واقعیت
- دکمهٔ «منو» از پدها حذف می‌شود؛ فقط کنترل بازی می‌ماند

**چرا یک فایل کافی است:** فایل دنیا از قبل همه‌چیز را نگه می‌دارد، پس
انتشار چیز تازه‌ای اختراع نمی‌کند.

**تأیید کامل:** یک بازی ساختم، Publish زدم، سرور ویرایشگر را **خاموش**
کردم، و بستهٔ منتشرشده را جداگانه با `play.sh` اجرا کردم. `/bench` جواب
۴۰۴ داد، API رد کرد، و بازی مستقیم در PLAY باز شد — سه بار فاصله زدم و
`SCORE 3` و `YOU WIN` روی صفحه آمد.

**تست:** ۲ تست جدید (۴۰۱ از ۴۰۱) و هر سه جهش عمدی بار اول گرفته شدند.

## نسخهٔ 0.23.0 — صفحهٔ بازی: رابط کاربری خودِ بازی

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

تا حالا قواعد می‌توانستند امتیاز بشمارند ولی بازیکن فقط یک خط متن سادهٔ
موتور را می‌دید. حالا خودت صفحهٔ بازی را می‌چینی.

**چهار نوع پنل:** برچسب، نوار (جان/انرژی)، جعبه (پس‌زمینه)، و دکمه.

**متن زنده:** `SCORE {score}` روی صفحه می‌شود `SCORE 1250` — بدون یک خط کد.
متغیر ناموجود **جایش خالی می‌ماند** نه اینکه آکولاد را به بازیکن نشان بدهد؛
اشتباه تایپی باید غلط به نظر برسد نه خراب.

**مکان با کسر است نه پیکسل** (۰ تا ۱)، پس چیدمانِ ساخته‌شده روی گوشی روی
دسکتاپ هم درست است و با تغییر اندازه به‌هم نمی‌ریزد.

**دکمه‌ها به قواعد وصل می‌شوند:** دکمه رویدادی بلند می‌کند و قاعده به آن
گوش می‌دهد. **پنل رویی برنده است** (کاربر دکمه‌ای را می‌زند که می‌بیند) و
**برچسب دکمه نیست** هرچقدر شبیهش باشد. هر پنل `visible` دارد، پس قاعده
می‌تواند صفحهٔ پیروزی را فقط در پایان نشان بدهد.

**تأیید زنده:** یک صفحهٔ کامل چیده شد — نوار بالا، `SCORE 1250`، `LIVES 2`،
نوار جان سبز و دکمهٔ PAUSE — همه فقط با پر کردن فرم.

**یک هشدار اشتباه که خودم پیگیری کردم:** در اجرای اول صفحه `SCORE score`
نشان داد و فکر کردم رمزگشایی URL موتور خراب است. مستقیم آزمایش کردم و
`urlDecode` **کاملاً درست** بود: `curl` من آکولادها را رمزگذاری نکرده بود.
موتور بی‌تقصیر بود و اگر بررسی نکرده بودم، کد سالم را «اصلاح» می‌کردم.

**تست:** ۱۰ تست جدید (۳۹۹ از ۳۹۹) و هر سه جهش عمدی بار اول گرفته شدند.

## نسخهٔ 0.22.0 — نقشه‌ها و صحنه‌ها

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

دو چیزی که بدون آن‌ها فقط می‌شد «یک اتاق» ساخت نه یک بازی.

**Blueprint:** جسمی را یک‌بار کامل تنظیم می‌کنی — فیزیک، برچسب، صدا، بافت —
و بعد با یک کلیک هر بار در صحنه می‌کوبی‌اش. **همه‌چیز همراهش می‌آید**، نه
فقط شکل؛ همین تمام دلیل ذخیره کردنش است. دو کپی **دو جسم‌اند**
(`Barrel`، `Barrel_2`)، وگرنه قاعدهٔ «Barrel را نابود کن» مبهم می‌شد. ذخیره
روی نام موجود **ویرایش** است نه ساختن دومیِ غیرقابل‌تشخیص.

**Stage:** یک بازی یعنی منو، مرحله و صفحهٔ پیروزی. هر صحنه دنیای خودش را
دارد و جابه‌جا شدن کار را دور نمی‌ریزد. صحنهٔ تازه با کف خالی شروع می‌شود
(ویرایشگر نباید روی هیچ باز شود) و صحنه‌ای که رویش ایستاده‌ای پاک نمی‌شود.
کنشِ `scene` در قواعد حالا واقعاً جابه‌جا می‌کند — بدون آن چند صحنه داشتن
بی‌فایده بود.

**طراحی:** نقشه **همان `EntityData`** است و صحنه **همان `Scene`**؛ هیچ مدل
جدیدی اختراع نشد، پس هرچه روی یک جسم کار می‌کند روی نقشه هم کار می‌کند.
`Scene` عمداً کپی‌ناپذیر است، پس جابه‌جایی صحنه صریحاً `clone()` صدا می‌زند.

**تأیید زنده:** یک بشکه تنظیم شد، Keep شد، و چهار کپی کوبیده شد — هر چهار
با بافت، فیزیک و برچسب خودشان روی صفحه.

**تست:** ۴ تست جدید (۳۸۹ از ۳۸۹). سه جهش امتحان شد و **یکی گرفته نشد**:
تستِ «صحنه کارش را نگه می‌دارد» فقط **اولین** جابه‌جایی را می‌سنجید، که از
مسیر دیگری در کد می‌گذرد. تست گسترش یافت تا بعد از بازگشت هم ویرایش کند و
دوباره جابه‌جا شود — حالا جهش گرفته می‌شود.

## نسخهٔ 0.21.0 — ویوپورت زنده: لمس و کشیدن روی صحنه

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

دومین قابلیت نقشهٔ راه: به‌جای تایپ کردن عدد، روی تصویر می‌زنی و می‌کشی.

- **ضربه انتخاب می‌کند** — جسم زیر انگشت، با باز شدن Dossier‌اش.
- **کشیدن جابه‌جا می‌کند** — روی صفحهٔ هم‌ارتفاع خودِ جسم، نه در هوا به سمت
  دوربین. چیزی که روی میز است روی میز می‌ماند.
- **چسبیدن به شبکه** (خاموش/۰٫۲۵/۰٫۵/۱) فقط X و Z را گرد می‌کند.

سه تصمیم که با اندازه‌گیری گرفته شدند:
- **نزدیک‌ترین برنده است** و ترتیب فهرست صحنه در جواب اثر ندارد.
- **زمین انتخاب نمی‌شود** — پس‌زمینه است و هر ضربهٔ خطا را می‌بلعید.
- **اجسام نازک قابل لمس‌اند** — جسم یک‌سانتی‌متری جعبهٔ حداقلی می‌گیرد؛
  ویرایشگری که نتوانی در آن چیزی را انتخاب کنی خراب است.
- **کشیدن به سمت افق رد می‌شود** — پرتوی تقریباً موازی زمین، صدها متر
  آن‌طرف‌تر به زمین می‌رسد و جسم را از نقشه پرت می‌کرد.

**معماری:** `Picking` هندسهٔ خالص است — نه رندرر می‌شناسد نه `WorldEditor`.
برنامه دوربین را هر فریم می‌دهد و **تصمیم** در موتور گرفته می‌شود، جایی که
بدون یک پیکسل رندر تست می‌شود.

**تأیید زنده:** ضربه روی مرکز تصویر توپ را انتخاب کرد، ضربه روی آسمان هیچ،
و کشیدن توپ را به `x=-1.0, z=1.0` برد — گرد شده روی شبکهٔ ۰٫۵.

**تست:** ۸ تست جدید (۳۸۵ از ۳۸۵). سه جهش عمدی امتحان شد و **دو تا گرفته
نشدند**، که دو تست بی‌فایده را لو داد:

۱. تست «نزدیک‌ترین برنده است» دو جعبه داشت که فقط **به نظر** روی هم بودند —
   پرتو هرگز به دوری نمی‌رسید، پس ابهامی برای حل کردن وجود نداشت. حالا هر
   دو عمداً روی یک پرتو چیده می‌شوند و تست خودش این را بررسی می‌کند.
۲. تست «کشیدن ارتفاع را حفظ می‌کند» `delta.y == 0` را می‌سنجید — که چون هر
   دو سر کشیدن روی یک صفحه‌اند، **همیشه** صفر است. حالا کشیدن روی صفحهٔ
   ارتفاع ۳ با صفحهٔ زمین مقایسه می‌شود.

## نسخهٔ 0.20.0 — منطق بصری: بازی‌سازی بدون کدنویسی

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

**هدف پروژه عوض شد.** مرحله‌بندی قدیمی (که تعیین می‌کرد چه بازی‌هایی ساخته
شود) حذف شد و `ROADMAP.md` بازنویسی شد: حالا هدف **قابلیت‌های یک موتور
بازی‌سازی کامل** است، با یک قانون یگانه — **قابلیت زیاد، بدون کدنویسی**.

اولین و مهم‌ترین قطعه ساخته شد: **منطق بصری**.

یک بازی مجموعه‌ای از **قاعده** است و هر قاعده جمله‌ای است که آدم می‌گوید:

```
WHEN key space   IF score < 10   DO add score 1   DO sound kick
```

**چرا فهرست و نه گراف نود:** گراف روی صفحهٔ گوشی خوانا نیست، و تقریباً همهٔ
منطق بازی واقعاً یک فهرست «وقتی این، آن» است. ترتیب را کاربر کنترل می‌کند و
بالایی برنده است.

**۱۰ رویداد** (`start`, `every-frame`, `key`, `collision`, `area-enter`,
`timer`, `event` و…) و **۱۴ کنش** (`add`, `move`, `spawn`, `destroy`,
`sound`, `animate`, `message`, `end-game` و…).

سه تصمیم که با آزمایش واقعی تثبیت شدند:
- **`area-enter` یک‌بار شلیک می‌کند** نه هر فریم؛ فرق «وارد شد» با «داخل
  است» تمام ارزش این رویداد است.
- **برخورد دوطرفه است** — «توپ به دیوار» و «دیوار به توپ» یک اتفاق‌اند.
- **رویدادِ بلندشده در همان فریم** به قاعده‌های بعدی می‌رسد، پس زنجیرهٔ چند
  قاعده یک‌جا حل می‌شود.

**معماری:** `LogicRuntime` می‌گوید **چه** باید بشود و دنیا انجامش می‌دهد.
این جدایی یعنی همهٔ منطق بدون رندر و فیزیک تست می‌شود.

**در Workbench:** دکمهٔ **Rules** با سه ستون — فهرست قاعده‌ها (روشن/خاموش،
بالا/پایین، حذف)، ساختن IF و DO، و متغیرها با وضعیت زندهٔ بازی.

**تأیید کامل:** یک بازی واقعی فقط با پر کردن فرم ساخته شد («فاصله را بزن،
سه امتیاز بگیر، ببر») و اجرا شد — `YOU WIN!` روی صفحه ظاهر شد.

**سه باگ واقعی که حین کار پیدا شدند:**
۱. **کلیدها هرگز به منطق نمی‌رسیدند** — `logicKeysPressed_` خوانده می‌شد
   ولی هیچ‌جا نوشته نمی‌شد، پس قاعدهٔ «وقتی کلید» هرگز شلیک نمی‌کرد.
۲. **پیام برنده در مسابقه نامرئی بود** — پیام را داخل یک شاخهٔ HUD گذاشته
   بودم، دقیقاً جایی که کسی ازش استفاده می‌کند.
۳. **قاعده‌ای با فاصله در نامش نصفه ذخیره می‌شد** — «on goal» به «on»
   برمی‌گشت، چون `escapeLineText` فاصله را محافظت نمی‌کند.

**تست:** ۱۶ تست جدید (۳۷۷ از ۳۷۷). سه جهش عمدی امتحان شد؛ **یکی گرفته
نشد** و نشان داد تست «بازی تمام‌شده متوقف می‌شود» در واقع چیزی را ثابت
نمی‌کرد (شکستِ داخل فریم جلوی رسیدن به آن کد را می‌گرفت). تست بازنویسی شد
تا واقعاً همان مسیر را بسنجد.

## نسخهٔ 0.19.0 — استخوان‌های خودت + رفع باگ بیلد Clang

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

### باگ بیلد که کاربر گزارش داد

بیلد روی گوشی با سه خطای `-Wsign-conversion` می‌شکست، در حالی که **هر سه
گیت من سبز بودند**. علتش را پیدا کردم و ریشه‌ای است، نه سطحی:

> `-Wconversion` در GCC برای ++C شامل `-Wsign-conversion` **نمی‌شود**، ولی
> در Clang می‌شود.

یعنی هر لغزش علامت‌دار/بی‌علامت روی گیت‌های GCC من تمیز کامپایل می‌شد و
بعد روی Clang گوشی می‌شکست. `-Wsign-conversion` حالا **صریحاً** در
`KIMIA_STRICT_WARNINGS` هست تا هر دو کامپایلر یک چیز بگویند. با روشن کردنش
یک باگ واقعی دیگر هم در ردیاب پرتو پیدا شد (اختلاط `u64` با `ULL`).

پوشه‌های `ThirdParty` هم `SYSTEM` شدند: هشدارهای stb مال ما نیست و با این
پرچم، تشخیص‌های خودمان را غرق می‌کرد.

### استخوان‌های خودت (مرحلهٔ ۳۵)

کاربر گفت: «اگه خودم استخوان‌های هر کاراکتر رو بذارم بهتره، دقیق‌تر میشه و
می‌خوام تنظیم کنم این خط مال کجای کاراکتر هست.»

حالا هر جسمی می‌تواند **استخوان‌های خودش** داشته باشد:

- `RigBone` عمداً «از اینجا تا آنجا» است نه چرخش و طول — چون آدم اندام را
  همین‌طور توصیف می‌کند و در ویرایشگر هم همین‌طور کشیده می‌شود. مختصات در
  فضای خود کاراکتر با پای روی `y=0`.
- هر استخوان `swing` خودش را دارد: ۰ یعنی بی‌حرکت (تنه، سر)، ۱ یعنی گام
  کامل، منفی یعنی فاز مخالف — که با آن دست‌ها برخلاف پاها تاب می‌خورند.
- **پنل Frame در Workbench:** فهرست استخوان‌ها؛ روی هرکدام بزنی مختصاتش در
  فیلدها می‌آید تا اصلاحش کنی. دکمهٔ **Default** اسکلت پیش‌فرض موتور را
  به‌صورت **استخوان‌های واقعی و قابل‌ویرایش** می‌ریزد — نقطهٔ شروع برای
  کشیدن، نه چیزی که مجبور باشی بپذیری.
- **Set bone جایگزین می‌کند نه اضافه**، چون کشیدن یک استخوان یعنی صدها بار
  فراخوانی با یک نام.
- **حذف استخوان، فرزندانش را یتیم می‌کند نه نابود** — حذف بی‌صدای کار
  کاربر بدتر است.
- ساختار دلخواه است: می‌شود دم، بال یا شش پا ساخت؛ فرزند با پدرش حرکت
  می‌کند.

با اندازه‌گیری تأیید شد: ریگ کاملاً غیرانسانی (دو بازوی بلند + دم، بدون
پا) درست ژست می‌گیرد، استخوانِ `swing = 0` **دقیقاً صفر** تکان می‌خورد، و
وقتی بدنه را متحرک می‌کنی دم **۰٫۵۷۹ متر** با آن می‌رود.

**تست:** ۴ تست جدید (۳۶۱ از ۳۶۱) — ریختن اسکلت پیش‌فرض به‌صورت مختصات
قابل‌ویرایش، جایگزینی به‌جای تکرار، یتیم شدن فرزندان، و بقای همه‌چیز پس از
save/load. دو جهش عمدی گرفته شدند.

## نسخهٔ 0.18.0 — بافت روی مدل

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

آخرین مورد از نقد کاربر: «بافت‌ها را استخراج می‌کنی» — ولی هیچ‌وقت **کشیده**
نمی‌شدند. واردکننده همیشه مسیر بافت را از `.mtl` یا از متریال‌های FBX بیرون
می‌کشید، اما هیچ‌کس تصویر را بار نمی‌کرد؛ هر مدلی هرچقدر هم با دقت بافت
خورده بود، به‌صورت یک رنگ تخت رندر می‌شد.

**حالا رسترایزر نرم‌افزاری بافت می‌کشد** — همان رسترایزری که روی گوشی کار
می‌کند:

- `RenderObject::texture` اضافه شد؛ `nullptr` یعنی همان رنگ تخت همیشگی،
  پس هرچه قبلاً کشیده می‌شد بیت‌به‌بیت همان می‌ماند.
- **UV با گوشه‌ها از برش صفحهٔ نزدیک عبور می‌کند**، وگرنه مثلثی که از لبهٔ
  دوربین رد شود تکهٔ اشتباهی از تصویر را می‌گیرد.
- **تصحیح پرسپکتیو**: `u/w` و `1/w` درون‌یابی می‌شوند و بعد تقسیم. بدون آن
  کف زمینِ بافت‌خورده با حرکت دوربین می‌لغزد و می‌پیچد.
- نمونه‌برداری نزدیک‌ترین همسایه با تکرار (wrap)، و **رنگ جسم بافت را رنگی
  می‌کند** پس رنگ تیم روی مدل بافت‌خورده هم کار می‌کند.
- گاما درست مدیریت می‌شود: تصویر گاما-کدشده است و سایه‌زنی خطی.

`loadMeshAsset()` هم اضافه شد تا فراخواننده لازم نباشد بداند فایل OBJ است
یا FBX. اپ بافت هر مدل را **یک بار** بار می‌کند و کش می‌کند.

**نکتهٔ روشی — تست اولم دو بار بی‌فایده بود.** جهش‌آزمایی نشان داد که با
**برداشتن کامل تصحیح پرسپکتیو** تست‌های اول و دومم همچنان سبز می‌ماندند:
شمردن نوارها در هر ردیف و پیدا کردن درز، هیچ‌کدام چیزی را ثابت نمی‌کردند.
با شکستن عمدی کد اندازه‌گیری کردم و امضای واقعی را پیدا کردم: روی نواری که
از دوربین دور می‌شود، ریاضی درست **۱۰ نوار** قابل تشخیص می‌دهد و
درون‌یابی خطی **۱۶ نوار**. تست سوم همین را می‌سنجد و جهش را می‌گیرد.

**دارایی تست تازه:** `Tests/assets/crate.obj` + `crate.mtl` +
`crate_skin.png` — یک مکعب با UV که به یک بافت واقعی اشاره می‌کند، تا کل
زنجیره (OBJ → MTL → PNG → صفحه) آزموده شود.

**تأیید زنده:** مکعب را از راه Workbench وارد کردم و شطرنجی نارنجی با
حاشیهٔ تیره‌اش روی هر وجه، با پرسپکتیو درست، روی صفحه ظاهر شد.

**تست:** ۵ تست جدید (۳۵۷ از ۳۵۷) — کشیده شدن بافت به‌جای رنگ تخت، تصحیح
پرسپکتیو، رنگی شدن با رنگ جسم، تاب‌آوری در برابر مش بدون UV و تصویر خالی،
و خواندن مسیر بافت از یک مدل واقعی. هر سه جهش گرفته شدند.

## نسخهٔ 0.17.0 — کاراکترها استخوان گرفتند

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

کاربر گفته بود: «چرا نقش‌های الکی به کاراکترهایی می‌دهی که اصلاً
استخوان‌بندی ندارند؟» حق داشت — ماژول اسکلت مرحلهٔ ۲۵ **صفر ارجاع** در
`Engine/World` داشت و بازیکن‌ها جعبه‌های لغزان بودند.

**حالا آدمک‌اند.** `makeFigureRig()` یک اسکلت ۱۱ استخوانه می‌سازد و هر
بازیکن روی زمین با آن رسم و ژست‌دهی می‌شود:

- **راه رفتن** با تاب خوردن دست و پای مخالف؛ طول گام و آهنگ قدم هر دو با
  سرعت زیاد می‌شوند، پس دویدن شبیه راه‌رفتنِ تندشده نیست.
- **پرش**: پاها جمع می‌شوند. **افتادن** (آرنا): آدمک روی زمین می‌افتد.
- **ایستادن**: کاملاً بی‌حرکت.
- آدمک به سمتی که می‌دود **می‌چرخد**.

این یک **ریگ** است نه کاراکتر — قاعدهٔ «موتور محتوا نمی‌سازد» سر جایش
است و هر مدلی وارد کنی جایش را می‌گیرد.

**سه باگ که با اندازه‌گیری پیدا شدند، نه با حدس:**

۱. **ران طول نداشت.** به استخوان ران فقط جابه‌جایی عرضی داده بودم؛
   چرخاندن استخوانِ طول‌صفر هیچ چیزی را تکان نمی‌دهد، پس پاها اصلاً تاب
   نمی‌خوردند و آدمک نیم متر بالای زمین شناور بود.
۲. **پرش پاها را بلند نمی‌کرد** — ران می‌چرخید ولی زانو نه.
۳. **پروب خودم غلط بود:** شمارهٔ اشتباه قطعه را می‌خواندم و نتیجه گرفتم
   پاها تکان نمی‌خورند. آن شماره ران راست بود، نه پای چپ. اگر بررسی
   نکرده بودم، «باگ» اشتباهی را رفع می‌کردم.

**تأیید بصری:** دو فریم گرفتم و شمردم — **۵۸۹۳ پیکسل** بین دو لحظه عوض
شد، و در تصویر آدمک زرد وسط قدم با پاهای باز دیده می‌شود.

**تست:** ۶ تست جدید (۳۵۲ از ۳۵۲) که همان چیزهایی را می‌سنجند که چشم
می‌بیند: پا روی زمین، گام واقعی، دست مخالف پا، بزرگ‌تر شدن گام با سرعت،
بی‌حرکتی کامل در ایستادن، و چرخیدن به سمت حرکت. هر سه جهش عمدی — از جمله
بازگرداندن دقیق همان باگ ران — گرفته شدند.

## نسخهٔ 0.16.1 — راه رسیدن به Workbench

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

**گزارش کاربر:** موتور را اجرا کرد و گفت «این خو همونه».

کاملاً حق داشت. Workbench ساخته شده بود ولی **هیچ راهی به آن نبود**:
صفحهٔ اصلی حتی یک لینک به `/bench` نداشت. تنها راه ورود این بود که آدرس را
از قبل بدانی — که به درد هیچ‌کس نمی‌خورد. من در پیام قبلی آدرس را
درست توضیح ندادم و کاربر طبیعتاً همان صفحهٔ همیشگی را دید.

**رفع:** دکمهٔ «⚙ Workbench — ویرایشگر» بالای صفحهٔ بازی، و آدرس‌ها در
README کنار همان دستور اجرایی که کاربر واقعاً می‌زند.

**تست:** یک تست تازه (۳۴۶ از ۳۴۶) که می‌سنجد صفحهٔ بازی به `/bench` لینک
دارد — این باگ از آن دسته است که تست واحد هرگز نمی‌گیرد، چون هر دو تکه
جدا‌جدا درست کار می‌کردند و فقط **وصل** نبودند. دقیقاً همان اشتباهی که
کاربر قبلاً دربارهٔ اسکلت مرحلهٔ ۲۵ گرفته بود.

## نسخهٔ 0.16.0 — KIMIA Workbench: رابط کاربری ویرایشگر

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

رابط کاربری واقعی روی `http://127.0.0.1:8080/bench` — با دکمه‌های واقعی که
به کدهای واقعی مرحلهٔ قبل وصل‌اند.

**هویت خودش را دارد، کپی نیست.** واژگان از **کارگاه** آمده نه استودیوی
فیلم: `Rack` (فهرست اجسام)، `Dossier` (پنل یک جسم)، `Fittings` (قطعاتی که
پیچ می‌شوند)، `Labels`، `Wiring` (اتصال به دکمه)، `Bring in`/`Scrap`.
هیچ واژهٔ عاریتی در صفحه نیست — تست این را صریح می‌سنجد.

**چه کاری می‌شود کرد:** فایل `.obj`/`.fbx` وارد کردن با اندازهٔ دلخواه،
جابه‌جایی و رنگ، پیچ کردن فیزیک (`static`/`dynamic`/`sphere`) که
**بلافاصله** اثر می‌کند، برچسب زدن، وصل کردن انیمیشن و صدا به دکمه یا
رویداد بازی، و **کشیدن سیم همان‌جا** برای آزمایش بدون ترک ویرایشگر.

**معماری:** صفحه هیچ‌چیز از موتور نمی‌داند. سرور یک قلاب عمومی `/api/*`
گرفت، لایهٔ `studio` سؤال‌های واقعی از `WorldEditor` می‌پرسد و JSON
برمی‌گرداند. هر تصمیمی در موتور می‌ماند جایی که تست‌پذیر است.

صفحه **کاملاً خودکفاست**: نه CDN، نه فونت بیرونی، نه فایل جانبی — روی گوشی
بدون اینترنت مثل دسکتاپ کار می‌کند. چیدمان سه‌ستونی روی موبایل تا می‌شود.

**دو باگ واقعی که با اجرای زنده پیدا شدند، نه با تست:**

۱. **قفل‌شدگی (lock-order inversion).** حلقهٔ اصلی قفل دنیا را می‌گرفت و
   بعد سرور را صدا می‌زد، در حالی که درخواست API برعکس عمل می‌کرد.
   `/api/pulse` کاملاً هنگ کرد. حالا حلقه قفل را **قبل از** تماس با سرور
   رها می‌کند.
۲. **عدد بی‌معنی در Dossier.** مقیاس خام برای مدل واردشده معنایی ندارد:
   بعد از اینکه `bring-in` فایل را به اندازه می‌نشاند، «۳» یعنی سه برابر
   فایل اصلی نه سه متر. حالا Dossier اندازهٔ **اندازه‌گیری‌شده** به متر را
   نشان می‌دهد. با رندر واقعی دیدم که جسم غول‌پیکر شده بود و همان لو داد.

**تست:** ۹ تست جدید (۳۴۵ از ۳۴۵) که همان کارهای کاربر را از راه API انجام
می‌دهند: وارد کردن مدل، پیچ کردن بدنه و **بررسی اینکه دنیای فیزیک واقعاً
عوض شد**، برچسب‌های گروهی، وصل کردن به دکمه و کشیدن سیم، و رد کردن مؤدبانهٔ
درخواست‌های بی‌معنی. هر دو جهش عمدی گرفته شدند.

## نسخهٔ 0.15.0 — کامپوننت، تگ و تریگر (پایهٔ رابط کاربری)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

**نقد کاربر درست بود.** او گفت نمی‌تواند جسم خودش را وارد کند، به آن فیزیک
و حرکت بدهد، انیمیشن را به دکمه وصل کند و صداها را تنظیم کند — و اینکه
«نقش‌های الکی به کاراکترهایی می‌دهی که اصلاً استخوان‌بندی ندارند».

بررسی با `grep` حرف او را تأیید کرد. اعداد شرم‌آور بودند:

| ادعا | واقعیت قبل از این نسخه |
|---|---|
| اسکلت/انیمیشن (مرحلهٔ ۲۵) | **صفر** ارجاع در `Engine/World` — کتابخانهٔ مرده |
| بافت روی مدل | **صفر** استفاده در اپ |
| مدل واردشده فیزیک بگیرد | نداشت |
| انیمیشن به دکمه | **صفر** |
| سیستم تگ | نداشت |

من مرحله‌به‌مرحله قابلیت ساخته بودم بدون اینکه به هم وصلشان کنم، و ۳۲۹ تست
سبز هیچ‌کدام این را نگرفت چون همه **واحدهای جدا** را می‌سنجیدند نه اینکه
«آیا کاربر می‌تواند کاری بکند».

**این نسخه پایه را درست می‌کند.** رفتار یک جسم دیگر از **نامش** نمی‌آید.

- **`BodyComponent`** — هر جسمی (از جمله مدلی که خودت وارد کرده‌ای) می‌تواند
  `static`، `dynamic` یا `sphere` باشد با جرم، اصطکاک و ارتجاع خودش.
  **کامپوننت بر نام غلبه می‌کند**: قبلاً فقط `Crate_*` هل داده می‌شد و
  `Block_*` جامد بود؛ حالا هر چیزی می‌تواند هر چیزی باشد.
- **تگ‌ها** — `enemy`، `cover`، `goal`. یک تگ به یک **گروه** اشاره می‌کند
  بدون اینکه نام هیچ‌کدام نوشته شود.
- **`AnimationComponent` و `SoundComponent`** — با یک `trigger` که یا نام
  کلید است (`k`) یا رویداد داخلی بازی (`goal`, `kick`, `tackle`). یعنی
  **وصل کردن انیمیشن به دکمه، بدون یک خط کد**.
- **API ویرایش** که رابط کاربری با آن حرف می‌زند: `importModel`,
  `setEntityBody`, `addEntityTag`, `addEntityAnimation`, `entitiesWithTag`,
  `fireTrigger` و بقیه — همه با **نام جسم**، پس UI می‌تواند رشته‌ای بماند.
- **اثر فوری**: تغییر کامپوننت بلافاصله فیزیک را بازمی‌سازد. ویرایشگر باید
  نتیجه را نشان بدهد نه اینکه بگوید دوباره اجرا کن.

**تأیید عملی زنجیرهٔ کامل** (نه فقط تست واحد): `spider.obj` وارد شد →
فیزیک `dynamic` گرفت → تگ `enemy` خورد → انیمیشن به کلید `k` وصل شد →
فشار دکمه آن را اجرا کرد و صدا را صف کرد → و **همه‌چیز از save/load سالم
بیرون آمد**.

**سازگاری کامل با گذشته:** دنیایی بدون کامپوننت بایت‌به‌بایت مثل قبل ذخیره
می‌شود، پس هر فایل `.kimia` موجود دست‌نخورده کار می‌کند. تست این را صریح
می‌سنجد.

**تست:** ۷ تست جدید (۳۳۶ از ۳۳۶) که همان کارهای کاربر را انجام می‌دهند، نه
واحدهای جدا. هر سه جهش عمدی گرفته شدند.

یک باگ واقعی هم پیدا شد: `ctest` از پوشهٔ بیلد اجرا می‌شد و تست‌هایی که
فایل واقعی می‌خوانند شکست می‌خوردند (اجرای مستقیم سبز بود و پنهانش می‌کرد).
با `WORKING_DIRECTORY` درست شد.

**قدم بعدی:** خودِ رابط کاربری — صفحهٔ `/editor` با پنل سلسله‌مراتب و
Inspector که همین کامپوننت‌ها را نشان می‌دهد و ویرایش می‌کند.

## نسخهٔ 0.14.0 — حالت آرنا (مرحلهٔ ۳۰، بخش اول)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

نقشهٔ راه برای مرحلهٔ ۳۰ می‌گوید «حالت **آرنا** اول». همان ساخته شد: با
`arena on`، همان موتور فوتبال یک **تیراندازِ سوم‌شخص** می‌شود. فقط
`battleground` روشنش دارد.

**پیش‌نیاز واقعی: پرتوافکنی در فیزیک.** بدون آن تیراندازی ممکن نیست.
`PhysicsWorld::raycast()` نزدیک‌ترین برخورد را می‌دهد و می‌گوید به کاراکتر،
جعبهٔ ثابت یا زمین خورده. شلیک یک **پرتو** است نه گلوله: گلوله یک آرنای
۴۰ متری را در چند میلی‌ثانیه می‌پیماید، پس شبیه‌سازی پروازش فقط راهی گران
برای کشیدن یک خط راست است.

**سلاح از پروفایل می‌آید:** `weapon <جان> <خشاب> <نرخ> <آسیب> <برد> <پرکردن>`.
موتور سلاح را می‌دهد، پروفایل فقط عدد — **چیزی برای خریدن نیست**، پس
pay-to-win هم نیست.

**قواعدی که بازی را بازی می‌کنند:** نرخ شلیک (ماشهٔ نگه‌داشته خشاب را در یک
فریم خالی نمی‌کند)، **آتش خودی آسیب نمی‌زند**، خشاب خالی خودش پر می‌شود،
افتاده‌ها بعد از سه ثانیه سالم در انتهای زمین خودشان برمی‌گردند و به افتاده
شلیک نمی‌شود.

**دو چیز که با اندازه‌گیری کشف شدند و بدون آن‌ها بازی خراب بود:**

۱. **آرنا بدون پوشش یک تیرِ خلاص‌خانه است.** جعبهٔ خالی **۱۸۳ کشته در
   دقیقه** تولید می‌کرد. حالا آرنا خودش ۹ بلوک می‌سازد که هم جلوی بدن را
   می‌گیرند هم جلوی گلوله را. بعدش: **۵۸ کشته در دقیقه با ۱۸٪ دقت** —
   یک درگیری واقعی.
۲. **بازیکن‌ها هنوز نقش فوتبال بازی می‌کردند.** یک «دروازه‌بان» روی خط
   دروازه در یک درگیری فقط یک هدف آسان است. حالا `aiTargetFor` مسیر
   جداگانه دارد: نزدیک‌ترین دشمنِ سرپا، توقف در فاصلهٔ درگیری، و پخش شدن
   عرضی به‌جای صف تک‌نفره.

تنظیم سلاح هم اصلاح شد: پنج اصابت با نرخ ۸ در نیم ثانیه می‌کشت؛ حالا شش
اصابت با نرخ ۶ یعنی **زمان کشتن یک ثانیه** که جا برای واکنش و پوشش‌گرفتن
می‌دهد. نشانه‌گیری AI با مهارت بد می‌شود (AI کامل تیرانداز بی‌خطاست و هیچ
آدمی نمی‌بَرد) و لرزشش **قطعی** است.

**ورودی و تصویر:** `f` شلیک نگه‌داشتنی، `r` خشاب، با پدهای لمسی مخصوص خودش.
HUD جان و خشاب را کنار تعداد کشته‌ها می‌گذارد، رد تیر آخر کشیده می‌شود، و
افتاده‌ها خاکستری و خوابیده رسم می‌شوند.

**تست:** ۱۴ تست جدید (۳۲۹ از ۳۲۹) — ۴ تا برای پرتوافکنی (نزدیک‌ترین برخورد،
نُرمال درست، رد کردن تیرانداز، سد شدن با پوشش، زمین، پرتو صفر) و ۱۰ تا برای
آرنا. هر سه جهش عمدی گرفته شدند.

**سه باگ در تست‌های خودم** که موتور را تبرئه کردند: هر `PhysicsWorld` یک
کاراکتر در مبدأ دارد، پس پرتوی من از **داخل یک بدن** شروع می‌شد؛ و دو تست
دوئل از وسط آرنا شلیک می‌کردند که دقیقاً جای یک بلوک پوشش است — یعنی
موتور درست کار می‌کرد و تست اشتباه بود.

**آنچه هنوز نیست:** بتل گراند کامل (حلقهٔ جمع‌شونده، فرود، غارت، ۴۸ بازیکن)
که بخش دوم همین مرحله است.

## نسخهٔ 0.13.2 — حالا واقعاً تا گل می‌روند

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

**گزارش کاربر:** «بازم همونه، فقط درست جاگیری کردن.»

کاملاً درست. رفع قبلی جاگیری را درست کرده بود ولی **هیچ‌کس گل نمی‌زد** —
و در همان اجرای قبلی نتیجه ۰-۰ بود که باید توجهم را جلب می‌کرد. تست من
فقط «توپ تکان می‌خورد» را می‌سنجید، نه «گل می‌زنند».

**پنج باگ پیدا شد، همه با اندازه‌گیری نه حدس:**

۱. **فرارِ دیوار، دروازه را هم دیوار حساب می‌کرد.** این را خودم در رفع قبلی
   اضافه کرده بودم. مهاجم هر بار به `z=6.65` می‌رسید و برمی‌گشت — و آن عدد
   دقیقاً `halfLength - radius - kAiWallEscape` بود. حالا فقط دیوارهای
   **کناری** تله‌اند؛ انتهای زمین جایی است که دروازه است.

۲. **توپ هرگز هدف نداشت.** مهاجم به «جلوتر از توپ» می‌دوید نه به دروازه.
   حالا `aiGoalMouth()` دروازهٔ واقعی صحنه را پیدا می‌کند.

۳. **مهاجم توپ را به عقب می‌کشید.** به‌جای رفتن به سمت دروازه، حالا از
   **پشت** توپ روی خط توپ-دروازه حرکت می‌کند و از داخلش رد می‌شود.

۴. **تکل همیشه توپ را به یک سمت می‌کوبید.** هر تکل توپ را به سمت زمین
   حریفِ تکل‌زننده می‌فرستاد؛ چون یک تیم یک نفر بیشتر دارد، این سوگیری جمع
   می‌شد تا جایی که توپ **۹۲٪ مسابقه در یک نیمه** بود و فقط یک تیم می‌توانست
   گل بزند (۰-۲۲). حالا تکل توپ را **دور می‌کند** و کمی به سمت وسط زمین
   خم می‌شود تا مدافعِ کنار خط هر بار اوت نسازد.

۵. **آفساید هر فریم بررسی می‌شد.** به‌محض اینکه AI شروع به دوندگی کرد، پرچم
   **۴۴ بار در دقیقه** بالا می‌رفت و چمن **۹۵٪ وقت متوقف** بود. حالا داور
   خطی فقط توپی را می‌بیند که **خودِ بازیکن** پاس می‌دهد.

به‌علاوه: مهاجم داخل ۴٫۵ متری دروازه **شوت** می‌زند (دریبل کردن تا داخل نت
همیشه به دفاع فرصت برگشت می‌داد)، به سمت **تیرک** می‌زند نه وسط دروازه که
دروازه‌بان ایستاده، و پشتیبان‌ها از مسیر حاملِ توپ کنار می‌کشند.

**نتیجهٔ اندازه‌گیری‌شده (سه دقیقه، شروع از نقاط مختلف):**

| | قبل | بعد |
|---|---|---|
| خیابونی | ۰-۰ (هیچ‌کس نمی‌رسید) | **۱۰-۰، ۷-۱، ۵-۴** |
| چمن | ۰-۰، ۹۵٪ متوقف | **۵-۳، ۱-۴، ۰-۵، صفر توقف بی‌مورد** |
| توپ در یک نیمه | ۹۲٪ | **۳۹٪** |

نتایج با نقطهٔ شروع تغییر می‌کنند، یعنی بازی واقعی است نه حلقهٔ ثابت.

**تست:** ۵ تست جدید (۳۱۵ از ۳۱۵) که مستقیماً همین گزارش را قفل می‌کنند —
«باید گل زده شود»، هدف‌گیری دروازهٔ درست، جلو رفتن توپ نه عقب، نداشتن
سوگیری یک‌طرفه، و سوت نخوردن بازی وقتی انسان توپ را لمس نکرده. هر سه جهش
عمدی گرفته شدند.

**نکتهٔ روشی:** رفع قبلی‌ام ناقص بود چون تستش ضعیف بود. «توپ حرکت می‌کند»
شرط کافی برای «بازی کار می‌کند» نیست؛ شرط درست **«گل زده می‌شود»** است.

## نسخهٔ 0.13.1 — رفع قفل‌شدن بازیکن‌های کامپیوتری

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

**گزارش کاربر:** «هوش مصنوعی‌ها به همدیگه گیر می‌کنن، نمی‌رن تا گل.»

کاملاً درست بود. بازتولیدش کردم و بدتر از گزارش بود: بعد از حدود ۵ ثانیه
توپ **برای همیشه** یخ می‌زد و مسابقه تمام‌شده بود.

**چهار باگ ریشه‌ای پیدا شد، نه یکی:**

۱. **مالکیت انحصاری نبود.** هر دو تیم هم‌زمان فکر می‌کردند توپ را دارند، پس
   هر دو «حمله» می‌کردند و توپ را در **دو جهت مخالف** می‌کشیدند. نتیجه:
   دقیقاً همان گیر کردن به همدیگر. حالا مالکیت با نزدیک‌ترین بازیکن است و
   فقط یک طرف می‌تواند داشته باشدش.

۲. **مهاجم به دروازهٔ خودش شوت می‌زد.** کد تکل برای بازیکنِ صاحب توپ هم
   اجرا می‌شد، پس تیمِ دارندهٔ توپ هر فریم توپ خودش را به دیوارِ ته زمین
   می‌کوبید و آنجا می‌چسبید. حالا **فقط مدافع** تکل می‌زند.

۳. **اسلات‌های تکراری.** انتخاب جای بازیکن با `id % 3` بود، پس در تیم
   ۵ نفره شماره‌های ۷ و ۱۰ **دقیقاً یک نقطه** می‌گرفتند و روی هم تلنبار
   می‌شدند. حالا هر بازیکن شمارهٔ خودش را داخل تیم می‌گیرد و کل عرض زمین
   بینشان تقسیم می‌شود.

۴. **فاصلهٔ شخصی وجود نداشت.** دو بازیکن می‌توانستند دقیقاً روی هم بایستند،
   هرکدام به دیگری فشار بیاورد و هیچ‌کدام تکان نخورد.

**آنچه اضافه شد:**

- **نقش‌های واقعی** (`aiRole`): `KEEPER` / `ATTACK` / `DEFEND` / `SUPPORT`.
  همان بازیکن وقتی توپ را دارد **حمله** می‌کند و وقتی ندارد **دفاع** —
  این جواب سؤال «چرا آن یکی دارد آنجا می‌دود؟» است.
- **دریبل واقعی:** بازیکن صاحب توپ آن را به سمت دروازهٔ حریف **می‌راند**.
  قبلاً توپ فقط وقتی تکان می‌خورد که کسی شانسی بهش می‌خورد.
- **فاصلهٔ شخصی:** بازیکن‌ها همدیگر را از خود دور می‌کنند؛ دو نفر که دقیقاً
  روی هم باشند با شمارهٔ زوج/فرد دو جهت مخالف را انتخاب می‌کنند تا هر دو
  منتظر هم نمانند.
- **تشخیص گیر کردن:** بازیکنی که می‌خواهد برود ولی جایی نمی‌رود، بعد از
  ۰٫۶ ثانیه **کنارگَردی** می‌کند تا دور مانع بچرخد.
- **فرار از دیوار:** توپِ چسبیده به دیوار به سمت وسط زمین برگردانده می‌شود.

**نتیجهٔ اندازه‌گیری‌شده (یک دقیقه فوتبالِ کاملاً کامپیوتری):**

| | قبل | بعد |
|---|---|---|
| بلندترین توقف توپ | **بی‌نهایت** | **۰٫۴۸ ثانیه** |
| مسافت توپ در محور طول | ~۰ (یخ‌زده) | **۱۲٫۸۲** از ۱۶ متر |
| جفت‌های روی‌هم‌افتاده | ۵ | ۰ |

**تست:** ۵ تست جدید (۳۱۰ از ۳۱۰) که مستقیماً همین گزارش را قفل می‌کنند —
یک دقیقه شبیه‌سازی با شرط «توپ باید بیش از نصف زمین را بپیماید و هرگز
بیش از ۳ ثانیه نایستد»، فاصلهٔ شخصی، یکتا بودن مسیر هر بازیکن، درستی
نقش‌ها و انحصاری بودن مالکیت، و بیرون کشیدن توپ از دیوار.

با جهش عمدی هر سه رفع اصلی تأیید شد. یک تست قدیمی («ازدحام») هم واقعاً
شکست چون حالا مهاجم توپ را حمل می‌کند؛ به‌جای شل کردنش، دقیق‌تر شد:
هیچ‌کس جز صاحب توپ حق ندارد رویش بایستد و هیچ دو بازیکنی نباید داخل هم
باشند.

## نسخهٔ 0.13.0 — قوانین چمن (مرحلهٔ ۲۹)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

چمن حالا **مسابقهٔ واقعی** است: اوت، آفساید، خطا و خستگی. و مهم‌تر از خودِ
قوانین، جایی است که **روشن نیستند** — کوچه داور خطی ندارد و متوقف کردن یک
بازی خیابونی برای اوت خرابش می‌کند.

**دو کلید تازهٔ پروفایل:** `rules on/off` و `stamina <نرخ>`. هر دو فقط در
چمن روشن‌اند (`on` و `0.55`)؛ بقیه دقیقاً مثل قبل بازی می‌کنند.

**قوانین:**

- **اوت** — توپ از خط طولی که بیرون برود، بازی می‌ایستد و از **کمی داخلِ**
  خط (نه رویش) دوباره شروع می‌شود. توپ مرده روی نقطه پارک می‌شود.
- **آفساید** — پاس به هم‌تیمیِ جلوترِ **آخرین مدافع**. سه نکته که واقعاً
  پیاده شدند: **هم‌سطح بودن آفساید نیست** (قانونی که همه اشتباه می‌گیرند)،
  در نیمهٔ خودی هرگز آفساید نیست، و یک حاشیهٔ کوچک هست تا یک تار مو آفساید
  حساب نشود.
- **خطا** — دویدن **به سمت** حریف با سرعت بیش از `3.4` متر بر ثانیه. ایستادن
  کنارش خطا نیست، آهسته رفتن سراغش (چالش منصفانه) خطا نیست، و دور شدن ازش
  با هر سرعتی خطا نیست.
- هر توقف سوت می‌زند، ترفند در جریان را می‌کُشد، و بعد از یک مکث کوتاه بازی
  دوباره زنده می‌شود. خط HUD مثل `THROW IN  ANHA BALL`.

**خستگی:** دویدن جان می‌گیرد و ایستادن برمی‌گرداند — ولی **بازگشت کندتر از
مصرف است**، پس نمی‌شود کل مسابقه دوید. بازیکن کاملاً خسته هنوز ۵۵٪ سرعت
دارد: خسته می‌شوی، از حرکت نمی‌ایستی. عدد واقعی: ۱۰ ثانیه دویدن ۱٫۰ → ۰٫۴۵
(سرعت ۴٫۰ → ۳٫۰۱)، و ۱۰ ثانیه استراحت → ۰٫۷۸.

**تست:** ۸ تست جدید (۳۰۵ از ۳۰۵) و یک قلاب تازهٔ `setSquadPosition()` تا
تست‌ها بتوانند دقیقاً وضعیتی را که می‌سنجند بسازند.

**جهش‌آزمایی این بار دو حفره در خودِ تست‌هایم پیدا کرد** و هر دو آموزنده
بودند:

۱. تست خطا بازیکن را **دقیقاً روی مرز** ۰٫۷ متری گذاشته بود، پس بررسی
   فاصله جلوی بررسی سرعت را می‌گرفت و شرط سرعت اصلاً سنجیده نمی‌شد. با
   جایگزینی «جاگینگ با نصف سرعت» درست شد.
۲. تلاش اولم برای رفع آن از `setFineMove` استفاده کرد — که کشف کردم **فقط
   روی ویرایشگر اثر دارد نه روی PLAY**. تست شکست، ولی باگ در موتور نبود؛
   فرض خودم غلط بود.

جهش سوم (حذف زودخروج خستگی) گرفته نشد و بررسی نشان داد **تغییر رفتاری
واقعی نیست** — چون `stamina = 0` نرخ را هم صفر می‌کند. کامنت کد اصلاح شد تا
ادعای نادرست نکند.

## نسخهٔ 0.12.0 — HUD مسابقه، دوربین پخش و صداهای تازه (مرحلهٔ ۲۸)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

**باگ واقعی که این مرحله پیدا کرد: تیم‌ها اصلاً کشیده نمی‌شدند.**

از مرحلهٔ ۲۱ بازیکن‌ها در فیزیک وجود داشتند ولی هیچ‌وقت **رسم** نمی‌شدند —
تا وقتی بی‌حرکت بودند کسی متوجه نشد. حالا که مرحلهٔ ۲۷ آن‌ها را به دویدن
انداخته، حریفِ نامرئی بازی را غیرقابل‌بازی می‌کند. با اجرای واقعی موتور و
نگاه‌کردن به فریم پیدا شد، نه با تست. حالا هر بازیکن یک جعبهٔ رنگی است:
**آبی** ما، **قرمز** آن‌ها، **زرد** دروازه‌بان‌ها.

**دوربین از اپ به موتور منتقل شد.** تصمیم دوربین قبلاً داخل
`WorldEditorApp.cpp` بود، یعنی **هیچ تستی نمی‌توانست بسنجدش**. حالا در
`WorldEditor` است: `cameraTarget()`، `cameraDistance()`، `cameraFollowsAim()`.

**کلید تازهٔ پروفایل:** `camera orbit/chase/broadcast`.

- `chase` — پشت نشانه می‌چرخد. گلف و بتل گراند.
- `broadcast` — **دوربین کنار زمین**: با باز شدن بازی عقب می‌کشد (۷ متر تا
  سقف ۱۸) تا توپ و بازیکن هر دو در کادر بمانند، و کادر را روی نقطه‌ای بین
  آن دو می‌گیرد با وزن **۶۵٪ به سمت توپ** چون بیننده آن را تماشا می‌کند.
  **پشت نشانه نمی‌چرخد** — دوربین تلویزیونی که با هر چرخش بازیکن دور خودش
  بچرخد قابل تماشا نیست. خیابونی و چمن.
- `orbit` — همان مدار سادهٔ ویرایشگر؛ سندباکس.

زوم دستی بازیکن (`q`/`e`) یادش می‌ماند و دوربین پخش رویش می‌نویسد نه اینکه
پاکش کند.

**HUD مسابقه:** خط `LAST 25S` در ثانیه‌های پایانی، مثل ساعت ورزشگاه که قرمز
می‌شود.

**سه صدای تازه روی همان مسیر `/sfx`:** سوت داور (کیک‌آف و پایان بازی)، تکل
(تاقِ کلفت‌تر و بم‌تر از ضربهٔ تمیز)، و ترفند (زینت کوتاه بالا‌رونده). قبلاً
ترفند صدای ضربهٔ معمولی می‌داد، یعنی نمایش‌دادن مثل یک لمس ساده به گوش
می‌رسید.

**تست:** ۸ تست جدید (۲۹۷ از ۲۹۷) — سبک دوربین هر چهار بازی، اینکه **فقط**
دوربین chase دنبال نشانه می‌چرخد، عقب کشیدن دقیق دوربین پخش با عدد واقعی و
سقفش، کادرگیری بین توپ و بازیکن، شمارش معکوس ثانیه‌های پایانی، سوت پایان،
و صدای اختصاصی ترفند و تکل.

با جهش عمدی تأیید شد: ثابت نگه داشتن فاصلهٔ دوربین پخش و چرخاندنش پشت نشانه،
هر دو گرفته شدند. گیت `-Werror=switch` هم سه رویداد تازه را که در اپ
مدیریت نشده بودند گرفت — دقیقاً کاری که سیاست صفر اخطار برایش هست.

## نسخهٔ 0.11.0 — بازیکن‌های کامپیوتری (مرحلهٔ ۲۷)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

تا حالا حریف‌ها مجسمه بودند. حالا **می‌دوند، جا می‌گیرند و توپ را از تو
می‌گیرند** — و همین تازه مرحلهٔ ۲۶ را معنادار می‌کند: لایی زدن وقتی ریسک است
که واقعاً کسی باشد که تنبیهت کند.

**کلید تازهٔ پروفایل:** `ai <مهارت>` از ۰ تا ۱. **۰ یعنی مجسمه**، یعنی دقیقاً
رفتار همهٔ نسخه‌های قبلی؛ پس هیچ دنیای قدیمی‌ای تکان نمی‌خورد. خیابونی
`0.6` گرفت (کوچه: مشتاق ولی نه حرفه‌ای)، چمن `0.85` (مسابقهٔ جدی، سریع
می‌بندنت)، بتل گراند `0.7`، گلف صفر چون اصلاً حریفی ندارد.

**یک ایدهٔ مرکزی: فقط _یک نفر_ از هر تیم دنبال توپ می‌رود.**

بدون این قاعده هر ۱۰ نفر هم‌زمان به توپ حمله می‌کنند و مسابقه می‌شود
فوتبال مدرسه — نه بازی‌ای که ارزش حریف‌بودن داشته باشد. پس:

- **مهاجم توپ (`aiChaser`)** — نزدیک‌ترین نفر به توپ، برای هر تیم دقیقاً یکی.
- **دروازه‌بان (`aiKeeper`)** — عمیق‌ترین نفر در نیمهٔ خودی. روی خط می‌ماند و
  فقط عرضی حرکت می‌کند؛ وقتی توپ نزدیک است کمی بیرون می‌آید. **هیچ‌وقت** برای
  توپ بالا نمی‌آید، چون دروازهٔ خالی از گل‌خوردن بدتر است. مهاجم توپ هرگز
  دروازه‌بان نیست.
- **بقیه** — پشت توپ در نیمهٔ خودی جا می‌گیرند و عرض زمین را پُر می‌کنند، پس
  تیم شکلش را حفظ می‌کند و کسی هست که بشود بهش پاس داد.

**تکل:** حریفی که به توپ برسد آن را به سمت زمین حریف می‌زند. قدرتش با مهارت
مقیاس می‌شود. حتی بهترین AI کمی از آدم کندتر است (`kAiMaxSpeedFactor = 0.92`)
تا بازیکن خوب همیشه بتواند از پسشان بربیاید.

**تست:** ۸ تست جدید (۲۸۹ از ۲۸۹) — مجسمه‌بودن **دقیق** در مهارت صفر، یکی‌بودن
مهاجم توپ و فرق داشتنش با دروازه‌بان در هر دو تیم، نزدیک شدن واقعی (۲٫۶۷ →
۰٫۳۰ متر)، ماندن دروازه‌بان در نیمهٔ خودش وقتی توپ آن سر زمین است،
**دقیقاً یک نفر** روی توپ (نه ازدحام)، تکل واقعی، و اینکه نمایش‌دادن جلوی
مدافع یا کامل می‌شود یا هیچ — هرگز نصفه‌امتیاز.

با **جهش دادن عمدی کد** تأیید شد: برداشتن قاعدهٔ «یک مهاجم» و فرستادن
دروازه‌بان دنبال توپ، هر دو گرفته شدند.

**چهار تست قدیمی واقعاً شکستند** و این خودش خبر خوبی بود: AI زنده حالا در
گل‌زدن، پاس، و مسیر توپ دخالت می‌کند. هیچ‌کدام باگ نبودند — آن تست‌ها دربارهٔ
دیوار/امتیاز/پیچ توپ‌اند نه دربارهٔ حریف، پس با قلاب تازهٔ `setAiSkill(0)`
حریف را کنار گذاشتند تا همان چیزی را بسنجند که برایش نوشته شده‌اند.

## نسخهٔ 0.10.0 — حرکات نمایشی (مرحلهٔ ۲۶)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

فوتبال خیابونی فقط گل زدن نیست. حالا موتور **لایی**، **رولت** و **روپایی**
دارد — با یک قاعدهٔ ساده که همه‌شان را جالب می‌کند: **ریسک**.

**کلید تازهٔ پروفایل:** `tricks on/off`. فقط **خیابونی** روشن است. کوچه جایی
است که لایی از گل هم باارزش‌تر است؛ مسابقهٔ جدی چمن خاموشش می‌کند و گلف اصلاً
حریفی ندارد. همان موتور، دو بازی متفاوت.

**سه حرکت، سه ریسک:**

| حرکت | زمان | امتیاز | چه می‌کند |
|---|---|---|---|
| لایی (`n`) | ۰٫۴۵ ثانیه | ۵۰ | توپ را از لای پای حریف رد می‌کند |
| رولت (`o`) | ۰٫۷۰ ثانیه | ۳۰ | نیم‌دور می‌چرخی و توپ را با خودت می‌بری |
| روپایی (`u`) | ۰٫۵۵ ثانیه | ۱۰ | توپ را بالا می‌اندازی؛ خالص نمایش |

**قاعده‌ای که این‌ها را بازی می‌کند:**

- حرکت **زمان می‌برد** و در آن مدت **متعهدی**: نه می‌شود وسطش حرکت دیگری شروع
  کرد، نه دریبل و ضربهٔ معمولی کار می‌کند. حرکت مالک توپ است.
- **امتیاز فقط موقع تمام‌شدن** داده می‌شود. اگر وسط نمایش توپ را از دستت
  بگیرند، حرکت لغو می‌شود و **هیچ** نمی‌گیری. این تمام ریسک ماجراست.
- **لایی حریف می‌خواهد.** بدون کسی جلوی رویت، پایی نیست که توپ از لایش رد
  شود — موتور اصلاً اجازهٔ شروع نمی‌دهد.
- توپ باید **روی پا** باشد؛ در هوا نمی‌شود لایی زد.

**ورودی:** سه پد لمسی تازه (لایی/رولت/روپایی) که **فقط** در بازی‌هایی که
`tricks on` دارند ظاهر می‌شوند، و کلیدهای `n`/`o`/`u`. این‌ها **تپ**‌اند نه
نگه‌داشتنی، چون به حرکت متعهد می‌شوی.

**HUD:** حین اجرا `NUTMEG!` و بعدش مجموع `STYLE 80`؛ وقتی حرکات خاموش است یا
هنوز چیزی نزده‌ای، اصلاً نمایش داده نمی‌شود.

**تست:** ۸ تست جدید (۲۸۱ از ۲۸۱) — روشن/خاموش بودن در سه بازی، رد شدن بدون
توپ و ممنوعیت شروع دوباره، بالا رفتن واقعی توپ در روپایی و امتیاز **فقط** در
انتها، رد شدن لایی بدون حریف و کار کردنش با حریف، نیم‌دور دقیق رولت، لغو شدن
با از دست دادن توپ، متن دقیق HUD و جمع شدن امتیاز، و ظاهر نشدن پدها در چمن.

با **جهش دادن عمدی کد** تأیید شد که تست‌ها دندان دارند: پرداخت امتیاز در
ابتدا و برداشتن شرط حریف، هر دو گرفته شدند. جهش دوم اول **گرفته نشد** و
نشان داد تست لایی حالت «حریفی نیست» را نمی‌سنجید؛ تست تقویت شد.

## نسخهٔ 0.9.0 — اسکلت و انیمیشن (مرحلهٔ ۲۵)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

بزرگ‌ترین قطعهٔ باقی‌مانده: مدل‌ها حالا **استخوان** دارند و **تکان می‌خورند**.
همان چیزی که پوستر موتور وعده‌اش را می‌داد (`ANIMATION`).

**ماژول تازه `Engine/Graphics/Skeleton`:**

- `Skeleton` یک آرایهٔ **تخت** از `Bone` است و پدر همیشه **قبل از** فرزند
  می‌آید، پس یک پیمایش رو به جلو کل اسکلت را ژست می‌دهد — نه بازگشت، نه
  مرتب‌سازی سر رندر. `isValid()` دقیقاً همین شرط را می‌سنجد.
- هر استخوان `restPose` (نسبت به پدر) و `inverseBindPose` دارد. معکوس
  ذخیره می‌شود چون پوست‌اندازی فقط همان را لازم دارد.
- `VertexSkin`: هر رأس تا **۴** استخوان (`kMaxBoneInfluences`) با وزن‌هایی
  که جمعشان ۱ است؛ `normalize()` خودش مقیاس می‌کند.
- انیمیشن: `BoneKey` / `BoneTrack` / `AnimationClip`. `sampleTrack()` بین
  کلیدها درون‌یابی می‌کند (خطی برای مکان و مقیاس، **slerp** برای چرخش، از
  راه کوتاه). `clipTime()` کلیپ حلقه‌ای را می‌پیچاند و کلیپ یک‌بارمصرف را ته
  کلیپ نگه می‌دارد.
- `samplePose()` استخوان‌هایی را که کلیپ چیزی درباره‌شان نگفته در **حالت
  استراحت** می‌گذارد، پس کلیپی که فقط دست را تکان می‌دهد پاها را سر جایشان
  می‌گذارد.
- `computeWorldMatrices()` → `computeSkinMatrices()` → `skinMesh()`، و
  `poseMesh()` که هر سه را یک‌جا انجام می‌دهد.
- **تضمین کلیدی:** در حالت bind، ماتریس پوست دقیقاً همانی است و مش
  **بیت‌به‌بیت** برمی‌گردد.

**واردکنندهٔ FBX:** `loadFBXSkinned()` اولین مش پوست‌دار فایل را با اسکلت و
**همهٔ** استک‌های انیمیشن می‌خواند.

- منحنی‌های FBX روی شبکهٔ ثابت **۳۰ فریم بر ثانیه** نمونه‌برداری می‌شوند تا
  پخش همه‌جا یکسان باشد.
- وزن‌ها متعلق به **نقطهٔ کنترل**‌اند نه رأس خروجی؛ ufbx آن‌ها را سنگین‌ترین
  اول مرتب می‌کند، پس چهار تای اول بهترین‌هایند و بعد دوباره نرمال می‌شوند.
- ریشهٔ صحنهٔ FBX **استخوان نیست** و حذف می‌شود، وگرنه هر اسکلتی یک استخوان
  بی‌نام اضافه می‌گرفت.
- استکی که هیچ استخوانی را تکان نمی‌دهد اصلاً تِرَک نمی‌سازد.

**دارایی تست:** `Tests/assets/skinned_bar.fbx` — یک میلهٔ دو استخوانه که
پایینش به استخوان ریشه و بالایش به استخوان نوک وصل است، به‌همراه یک کلیپ
یک‌ثانیه‌ای «Bend». با `Tools/make_skinned_fbx.py` ساخته می‌شود و بازتولیدش
بایت‌به‌بایت یکسان است.

**تست:** ۱۲ تست جدید (۲۷۳ از ۲۷۳) در فایل تازهٔ `SkeletonTests.cpp` —
اعتبارسنجی ترتیب پدر/فرزند، زنجیر شدن ماتریس‌ها در سلسله‌مراتب، همانی بودن
ماتریس پوست در حالت bind، درون‌یابی دقیق کلیدها، ۴۵ درجه دقیقاً وسط یک چرخش
۹۰ درجه، پیچیدن/نگه‌داشتن زمان کلیپ، دست‌نخوردن استخوان بدون تِرَک، نرمال
شدن وزن‌ها، و روی **خودِ فایل FBX**: دو استخوان با نام و والد درست، ۳۶ رأس
همه وزن‌دار، و خم شدن واقعی — پایین میله **دقیقاً صفر** تکان می‌خورد و نوکش
بیش از ۰٫۵ واحد. با خراب کردن عمدی پوست‌اندازی تأیید شد که تست‌ها آن را
می‌گیرند.

## نسخهٔ 0.8.0 — آب‌وهوا و روز/شب (مرحلهٔ ۲۴)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

هر چهار بازی تا حالا در یک ظهرِ خشکِ همیشگی اجرا می‌شدند. حالا ساعت و هوا
دارند — و هر دو روی **بازی** اثر می‌گذارند، نه فقط روی تصویر.

**پروفایل:** دو کلید تازه.

- `weather <باران> <خیسی>` — باران ۰ تا ۱، خیسی زمین ۰ تا ۱. مثل `wind`،
  خطِ نصفه‌نوشته کلاً نادیده گرفته می‌شود نه نصفه‌اعمال.
- `time <ساعت>` — ۰ تا ۲۴، پیش‌فرض ۱۲ (ظهر) که همان رفتار قبلی است.

هر بازی حال‌وهوای خودش را گرفت: گلف صبح ۹ خشک، خیابونی ۱۷ با کوچهٔ هنوز نمناک
(`0.25`)، چمن ۱۹:۳۰ زیر نورافکن با نم‌نم باران (`0.35 / 0.5`)، بتل گراند
غروب ۲۰:۳۰.

**فیزیک:** `setWetness()` و `gripFactor()`.

- زمین خیس اصطکاک تماس را کم می‌کند، پس توپ **بیشتر می‌رود**.
- خشک **دقیقاً `1.0`** برمی‌گرداند: دنیای خشک بیت‌به‌بیت مثل قبل است.
- خیسِ کامل `kWetMinGrip = 0.35` از چسبندگی را نگه می‌دارد — لیز می‌شود،
  بی‌اصطکاک نه.

**دنیا:** `raining()`, `night()`, `pitchWetness()`, `sunHeight()`,
`daylight()`, `skyHudText()`.

- باران خودش زمین را خیس می‌کند؛ خیسی نهایی **بیشترینِ** باران و خیسیِ
  پروفایل است.
- شب = قبل از ۶ یا از ۱۹ به بعد. `daylight()` یک قوس کسینوسی است، پس سپیده و
  غروب نرم‌اند، و کف `kWorldNightLight = 0.12` دارد چون نورافکن هست: شبِ
  کاملاً سیاه نداریم.
- خط HUD تازه مثل `19:30 NIGHT RAIN WET`، که وقتی هوا عادی است اصلاً نمایش
  داده نمی‌شود.
- **پاس هم هوا را حساب می‌کند:** وزن پاس با `gripFactor()` تنظیم می‌شود،
  وگرنه روی زمین خیس از پای هم‌تیمی رد می‌شد. این را خودِ تست مرحلهٔ ۲۳ گرفت.

**ردیاب پرتو:** `applyWorldSky()` جهت و شدت خورشید و روشنی آسمان را از ساعت
و باران دنیا می‌سازد؛ `kimia_raytrace` خودکار از آن استفاده می‌کند مگر با
`--sun` صریحاً چیز دیگری بدهی. تأیید عملی: **همان دنیا** در ساعت ۱۲ میانگین
روشنایی **۱۱۵٫۴۵** و در ساعت ۲۲ **۵۱٫۱۲** دارد.

**تست:** ۱۰ تست جدید (۲۶۱ از ۲۶۱) — رفت‌وبرگشت و بریدن کلیدها، آب‌وهوای هر
چهار بازی، دورتر رفتن توپ روی زمین خیس، مقدار دقیق `gripFactor`، برابری
بیت‌به‌بیت حالت خشک، مرزهای دقیق شب/روز، کم شدن نور با باران، رسیدن واقعی
خیسی از پروفایل به شبیه‌سازی، متن دقیق HUD، و آسمان ردیاب پرتو.

## نسخهٔ 0.7.2 — رفع باگ: ویدیو روی گوشی فقط یک عکس بود

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

گزارش کاربر بعد از ۰٫۷٫۱: «اینکه فقط عکسه». یعنی اصلاح قبلی (faststart +
Range) کافی نبود و **رمزگشای گوشی** اصلاً تصویر را باز نمی‌کرد.

**علت:** فایل با پروفایل `High` در ابعاد **۱۲۷۰×۷۲۶** و نرخ ۶٫۸ مگابیت بر
ثانیه کدگذاری شده بود. عرض ۱۲۷۰ مضرب ۱۶ نیست و پروفایل High با این نرخ،
همان چیزی است که رمزگشای سخت‌افزاری اندروید پس می‌زند: صدا پخش می‌شود و
تصویر نمی‌آید.

**اصلاح‌ها:**

- فیلم دوباره کدگذاری شد به شکل امن برای موبایل: **Constrained Baseline**،
  سطح ۳٫۱، **۱۲۸۰×۷۲۰** (مضرب ۱۶)، ۲ مگابیت بر ثانیه، کی‌فریم هر ثانیه،
  صدا AAC-LC روی ۴۴٫۱ کیلوهرتز، و باز هم `+faststart`. حجم از **۸٫۷** به
  **۲٫۷ مگابایت** رسید و مدت و محتوا دست‌نخورده ماند.
- **پوستر حذف شد.** تا حالا `poster="/logo.png"` روی ویدیو بود؛ وقتی
  رمزگشایی شکست می‌خورد، همان عکس ثابت می‌ماند و کاربر دقیقاً «فقط یک عکس»
  می‌دید. حالا پوستری در کار نیست تا خرابی پنهان نشود.
- **نگهبان (watchdog)** اضافه شد: صفحه هر نیم‌ثانیه `currentTime` را چک
  می‌کند و اگر ۳ ثانیه جلو نرفت، splash را می‌بندد و مستقیم می‌رود سر بازی.
  پس حتی روی گوشی‌ای که این کدک را هم نپذیرد، کاربر پشت یک تصویر ثابت گیر
  نمی‌افتد.

**تست:** ۱ تست جدید (۲۵۱ از ۲۵۱) که **خودِ فایل ارسالی** را می‌خواند: از
جعبهٔ `avcC` پروفایل و سطح را درمی‌آورد (باید ۶۶ = Baseline باشد) و از
`avc1` ابعاد را (باید ۱۲۸۰×۷۲۰ و مضرب ۱۶). با اجرا روی **فایل قدیمی نسخهٔ
۰٫۷٫۱** تأیید شد که تست آن را می‌گیرد (`profile == 66U` شکست). تست splash
هم به‌روز شد: حالا **نبودِ** poster و وجود نگهبان را الزام می‌کند.

## نسخهٔ 0.7.1 — رفع باگ: ویدیوی معرفی تصویر ثابت بود

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

گزارش کاربر: فیلم معرفی **صدا داشت ولی تصویرش ثابت** بود. دو علت داشت و هر
دو رفع شد.

**۱. فایل mp4 برای پخش جریانی چیده نشده بود.** در فایل اصلی، جعبهٔ فهرست
(`moov`) **آخر** فایل بود (آفست ۸٬۷۱۶٬۶۲۴) و دادهٔ تصویر (`mdat`) قبلش. تا
وقتی کل ۸٫۷ مگابایت نرسد مرورگر فهرست فریم‌ها را ندارد، پس صدا را پخش
می‌کند و تصویر می‌ماند. فایل با `-movflags +faststart` دوباره چیده شد:
اکنون `moov` در آفست ۳۲ است، پیش از `mdat`. خودِ ویدیو و صدا بازکدگذاری
نشدند (`-c copy`) — همان کیفیت اصلی.

**۲. سرور به درخواست‌های Range جواب نمی‌داد.** مرورگرها ویدیو را تکه‌تکه با
`Range: bytes=...` می‌گیرند. سرور همیشه کل فایل را با ۲۰۰ می‌داد، که برای
پخش ویدیو کافی نیست:

- پاسخ `206 Partial Content` با هدرهای `Content-Range` و `Content-Length`
  درست.
- پشتیبانی از بازهٔ معمولی (`bytes=0-99`)، بازهٔ باز (`bytes=990-`)، و بازهٔ
  بیرون‌زده که به انتهای فایل **بریده** می‌شود نه خطا.
- درخواست بی‌معنی → همان کل فایل با ۲۰۰، نه خطا.
- هدر `Accept-Ranges: bytes` روی همهٔ پاسخ‌ها.

**تست:** ۲ تست جدید (۲۵۰ از ۲۵۰). اولی بایت‌های دقیق هر بازه و هدرهایش را
می‌سنجد؛ دومی **خودِ فایل ارسالی** را باز می‌کند و جعبه‌هایش را می‌پیماید تا
مطمئن شود `moov` قبل از `mdat` و نزدیک ابتدای فایل است. با ساختن عمدی یک
فایل خراب (moov در انتها) تأیید شد که تست واقعاً آن را می‌گیرد.

## نسخهٔ 0.7.0 — کنترل توپ (مرحلهٔ ۲۳)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

تا حالا توپ فقط شوت می‌شد. حالا می‌شود آن را **برد**، **پیچ داد** و **پاس
داد**.

**فیزیک — چرخش و نیروی مگنوس:**

- `SphereBody::spin` (بردار محور-زاویه، رادیان بر ثانیه) و `magnusFactor`.
- نیرو: `kMagnusCoefficient * (spin × velocity)`، فقط **در هوا** — روی زمین
  حل‌گر تماس صاحب توپ است.
- چرخش در هوا با `kSpinAirDecay` آرام می‌شود و **فرود آمدن آن را می‌ساید**
  (`kSpinGroundKeep = 0.25`): چمن پیچ را می‌گیرد.
- `magnusFactor = 0` دقیقاً **بیت‌به‌بیت** برابر با نداشتن چرخش است — گارد
  اینکه هیچ دنیای موجودی تکان نخورد.

**دنیا — دریبل، پیچ، پاس:**

- **دریبل** یک مهارت **عمدی** است، مثل دکمهٔ دو نگه‌داشته می‌شود
  (`setDribbleHeld`). با آن، توپِ کنار پا به‌جای شوت شدن با سرعت خودِ
  بازیکن جلوی پایش رانده می‌شود و بالا هم نمی‌پرد. رها که باشد، هر ضربه
  **مو‌به‌مو** همان رفتار قبلی است.
- **پیچ**: استیک `setCurl(-1..1)` که سر ضربه به چرخش حول محور عمودی تبدیل
  می‌شود (`kWorldMaxCurl = 14` رادیان/ثانیه) و **خرج می‌شود** — برای ضربهٔ
  بعد باید دوباره بگیری.
- **پاس**: `pass()` نزدیک‌ترین هم‌تیمیِ **جلوی نشانه** (مخروط ۹۰ درجه) را
  پیدا می‌کند و توپ را با سرعتی می‌زند که با اصطکاک دقیقاً به پای او برسد.
  به حریف پاس نمی‌دهد و وقتی کسی نیست `false` برمی‌گرداند. `passTarget()`
  هدف فعلی را می‌گوید.

**کنترل‌ها:** نگه‌داشتن `c` = دریبل، نگه‌داشتن `q`/`e` = پیچ چپ/راست، زدن
`p` = پاس. دکمه‌های لمسی فقط جایی ظاهر می‌شوند که معنا دارند: پاس فقط وقتی
تیم هست، و در گلف فقط دو استیک پیچ کنار نشانه و شوت.

**تست:** ۹ تست جدید (۲۴۸ از ۲۴۸) — خم شدن توپ چرخان و تقارن دقیق دو طرف،
افت چرخش با مقدار دقیق یک گام و ساییده شدنش با فرود، برابری بیت‌به‌بیت
`magnusFactor = 0`، حمل توپ با دریبل در برابر شوت کامل، بریده شدن و خرج شدن
استیک پیچ، برد واقعاً متفاوت ضربهٔ پیچ‌دار، رسیدن پاس به پای هم‌تیمی، رد
شدن پاس وقتی کسی نیست، و درست ظاهر شدن پدها.

## نسخهٔ 0.6.0 — فیلم معرفی و هویت بصری

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

فیلم معرفی و لوگوی موتور (ساختهٔ V2zas Studio) وارد پروژه شدند و موقع باز
شدن بازی پخش می‌شوند — چه خود موتور، چه هر بازی‌ای که با آن ساخته شده.

**فایل‌ها:** `Branding/kimia-intro.mp4` (تیزر ۱۰ ثانیه‌ای، ۱۲۷۰×۷۲۶) و
`Branding/kimia-logo.png` (پوستر/لوگو).

**سرور وب (`Engine/App`):**

- دو مسیر تازه: `GET /intro.mp4` (video/mp4) و `GET /logo.png` (image/png)؛
  بدون فیلم، هر دو ۴۰۴ تمیز می‌دهند.
- `Server::setIntro(mp4, png)` و `Server::hasIntro()`.
- `web::loadIntroFrom(server, folder)`: پوشهٔ نام‌برده **قطعی** است؛ اگر
  خالی بدهی، `Branding`, `../Branding`, `../../Branding` را می‌گردد تا هم
  از درخت بیلد و هم از بستهٔ آفلاین کار کند. `"-"` یعنی صریحاً بدون فیلم.
  نبودن فیلم خطا نیست.
- صفحهٔ کنترل یک **splash** تمام‌صفحه گرفت: فیلم روی همه‌چیز پخش می‌شود، با
  دکمهٔ «رد کردن / SKIP»، و در همان تب دوباره تکرار نمی‌شود
  (`sessionStorage`). صفحه اول با یک درخواست HEAD مطمئن می‌شود فیلم واقعاً
  هست و تنها آن‌وقت splash را نشان می‌دهد؛ هر خطایی (۴۰۴، کدک، سیاست
  autoplay مرورگر) یعنی مستقیم برو سر بازی. پس موتور هیچ‌وقت پشت لوگوی
  خودش گیر نمی‌کند.
- فیلم بی‌صدا شروع می‌شود (وگرنه مرورگر پخش خودکار را رد می‌کند)؛ با یک
  لمس روی تصویر صدا وصل می‌شود.

**اپ‌ها:** `kimia_world` و `kimia_golf` خودشان فیلم را پیدا می‌کنند.
`kimia_world` دو گزینهٔ تازه دارد: `--branding DIR` و `--no-intro`، و موقع
اجرا می‌گوید `intro: yes` یا `intro: no`.

**بستهٔ آفلاین:** `Tools/package_release.sh` پوشهٔ `Branding/` را داخل بسته
می‌گذارد و `README.txt` فارسی توضیحش می‌دهد. آرشیو گلف حالا ۱۱ مگابایت است.
با واقعاً باز کردن بسته و اجرای آن تأیید شد که فیلم **بایت‌به‌بایت** همان
فایل اصلی سرو می‌شود.

**تست:** ۳ تست جدید (۲۳۹ از ۲۳۹) — سرو دقیق بایت‌ها و content-type و ۴۰۴
بدون برندینگ، وجود splash و دکمهٔ skip و پنهان بودن پیش‌فرضش، و رفتار
`loadIntroFrom` با پوشهٔ نام‌برده و با `"-"`.

## نسخهٔ 0.5.0 — مسابقه (مرحلهٔ ۲۲)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

مرحلهٔ ۲۱ زمین را پر از بازیکن کرد؛ حالا آن بازیکن‌ها یک **مسابقه** دارند:
امتیاز تیمی، ساعت، کیک‌آف و سوت پایان.

**پروفایل:** کلید جدید `match <ثانیه>` (۰ تا ۳۶۰۰؛ ۰ = بدون ساعت). یک
پروفایل وقتی مسابقه است که **هم** `team > 1` باشد **هم** `match > 0`.
مقادیر بازی‌های آماده: `street` ۳۰۰ ثانیه، `grass` ۶۰۰، `battleground`
۴۲۰، `golf` و زمین آزاد ۰ — پس گلف و زمین آزاد ذره‌ای عوض نشدند.

**دنیا:**

- امتیاز تیمی `scoreTeam1` / `scoreTeam2` (تیم ۱ = طرف بازیکن) کنار شمارندهٔ
  قدیمی `score` که دست‌نخورده ماند.
- **دروازه‌ها صاحب پیدا کردند:** دروازهٔ نیمهٔ `−Z` مال تیم ۲ است و گل در آن
  امتیاز تیم ۱؛ دروازهٔ نیمهٔ `+Z` برعکس. در حالت مسابقه عبور توپ از **هر دو
  طرف** خط دروازه گل حساب می‌شود، پس **گل به خودی** واقعاً کار می‌کند و به
  حساب حریف می‌رود.
- ساعت شمارش‌معکوس فقط در PLAY می‌رود (نه وسط جشن گل). `matchClock()`,
  `matchClockText()` («‪5:00‬»، «‪0:07‬» — به بالا گرد می‌شود تا `0:00` فقط
  یعنی تمام)، `matchOver()`, `matchWinner()` (۱، ۲، یا ۰ = مساوی).
- **کیک‌آف** `kickOff()`: توپ روی نقطهٔ وسط، بازیکن سر جای خودش و هر دو تیم
  دوباره در آرایش. بعد از هر گل و با «توپ از نو» اجرا می‌شود.
- سوت پایان → صفحهٔ «پایان دور» با `FULL TIME  MA 2 - 1 ANHA` و
  `MA BORDIM` / `ANHA BORDAND` / `DRAW`.
- HUD مسابقه: `MA 1 - 0 ANHA  4:37`. `statsLine` هم `| match 1-0 | clock
  4:37` می‌گیرد.
- فایل دنیا: خط `# match <ما> <آن‌ها>` فقط وقتی نوشته می‌شود که امتیازی وجود
  داشته باشد، پس هیچ فایل قدیمی تغییر نکرد؛ فایل بدون این خط ۰-۰ خوانده
  می‌شود.

**تست:** ۶ تست جدید (۲۳۶ از ۲۳۶) — شرط دوگانهٔ حالت مسابقه، شمارش‌معکوس و
قالب دقیق ساعت و سوت پایان، گل و کیک‌آف بعد از آن، گل به خودی، ذخیره/بارگذاری
امتیاز تیمی، و بریدن مقدار `match`.

## نسخهٔ 0.4.0 — چند کاراکتر (مرحلهٔ ۲۱)

**تاریخ:** ۱۴۰۴/۰۶/۱۵ (2026-09-06)

تا امروز دنیای فیزیک دقیقاً **یک** کاراکتر داشت. حالا N کاراکتر دارد و
پروفایل می‌گوید هر تیم چند نفر است. این پایهٔ مسابقهٔ مرحلهٔ ۲۲ است.

**فیزیک (`Engine/Physics`):**

- `CharacterBody` فیلد جدید `team` گرفت: ۰ = بی‌تیم (بازیکن تنهای گلف و
  زمین آزاد)، ۱ و ۲ دو طرف مسابقه. فیزیک فقط شماره را حمل می‌کند؛ قانون
  بازی جای دیگری است.
- API جدید: `addCharacter`, `removeCharacter`, `characterById`,
  `characterCount`, `characterIds` (مرتب صعودی) و اورلودهای id-دار
  `resetCharacter`, `moveCharacter`, `characterJump`.
- کاراکتر شمارهٔ ۱ (`kPrimaryCharacter`) همیشه هست و همان بازیکن است؛
  `character()` بدون تغییر به او اشاره می‌کند، پس کد قدیمی دست‌نخورده کار
  می‌کند.
- **برخورد کاراکتر با کاراکتر** روی هر سه محور: بازیکن از داخل هم‌تیمی رد
  نمی‌شود و می‌تواند روی سر دیگری بایستد (`characterSupported`).
- کاراکترها فضای شمارهٔ **جدا** از کره/جعبه دارند، پس id هیچ جسمی جابه‌جا
  نشد.
- `clear()` بازیکن ۱ را با همان مکانش نگه می‌دارد و فقط نفرات اضافه را
  پاک می‌کند — ساختن دوبارهٔ زمین نباید بازیکن را جابه‌جا کند.

**پروفایل (`Engine/Profile`):** کلید جدید `team N` (بازهٔ ۱ تا ۱۶، خارج از
بازه بریده می‌شود). پروفایل بدون این خط یعنی تک‌نفره. مقادیر بازی‌های
آماده: `street` = ۵، `grass` = ۱۱، `battleground` = ۴، `golf` و زمین آزاد
= ۱.

**دنیا (`Engine/World`):** با ورود به PLAY، `spawnSquads()` ترکیب را
می‌چیند: `N-1` هم‌تیمی کنار بازیکن (تیم ۱، نیمهٔ `+Z`) و `N` حریف (تیم ۲،
نیمهٔ `-Z`)، هر ردیف در یک‌سوم طول زمین و پخش‌شده در عرض زمین. متدهای
`teamSize`, `squadCount`, `squadIds`, `squadPosition`, `squadTeam`.

**تست:** ۷ تست جدید (۲۳۰ از ۲۳۰) — شماره و تیم کاراکترها، ردنشدن از داخل
یکدیگر، ایستادن روی سر، رفتار `clear()`، ترکیب ۵ نفرهٔ خیابونی، ۱۱ نفرهٔ
چمن، تنهاماندن گلف، و بریدن مقدار `team`.

## نسخهٔ 0.3.1 — انتشار نسخهٔ آفلاین گلف (ابزار بسته‌بندی) و رفع دو ایراد CLI

| | |
| --- | --- |
| **نسخهٔ موتور** | `0.3.1` (ابزار + رفع باگ = وصله +۱) |
| **تاریخ امروز** | یکشنبه ۱۵ شهریور ۱۴۰۵ — 2026-09-06 |
| **زمان توسعهٔ این نسخه** | حدود ۱ ساعت (۰۵:۳۰ تا ۰۶:۳۰ به وقت تهران) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۳۶ ساعت |
| **تست‌ها** | 223/223 (۱ تست جدید) — صفر اخطار در ۴ گیت + `ctest` سبز |

**چرا** — طبق نقشهٔ راه، بعد از کامل شدن مرحلهٔ ۲۰٫۵ نوبت **انتشار نسخهٔ
آفلاین گلف** بود. «انتشار» برای این موتور یعنی چیزی که بازیکن بتواند روی
گوشی یا کامپیوتر کپی کند و با یک دستور بازی کند — بدون کامپایلر، بدون Git،
بدون اینترنت.

**قاعدهٔ محتوایی رعایت شد:** بسته هیچ زمین آماده‌ای ندارد. آنچه منتشر
می‌شود **بازی‌سازِ** گلف است + پروفایل گلف؛ زمین را خود بازیکن با منوها
می‌سازد. ساخت محتوا کار کاربر است، نه موتور.

**نسخهٔ اصلی (`1.0.0`) عمداً زده نشد:** طبق قاعدهٔ همین دفترچه، `MAJOR` فقط
وقتی بالا می‌رود که **کاربر اعلام کند**. این نسخه ابزارِ انتشار را می‌دهد؛
اعلام انتشار با شماست.

**چه تغییر کرد**

- `feat(tools)`: **`Tools/package_release.sh`** — بستهٔ آفلاین خودکفا:
  باینری + پروفایل‌ها + `play.sh` (اجرای یک‌دستوره با پورت دلخواه) +
  `worlds/` + `assets/` + `licenses/THIRD-PARTY.txt` + `README.txt` فارسی
  برای بازیکن + `VERSION.txt` (نسخه، **کامیت Git**، و اگر درخت
  کامیت‌نشده باشد صریحاً `(uncommitted changes)`) + `MANIFEST.txt` با
  sha256 هر فایل. خروجی: `.tar.gz` حدود **۷۸۴ کیلوبایت**.
  اسکریپت عمداً سخت‌گیر است — بستهٔ خراب بدتر از نبودِ بسته است:
  - بیلد Release با `-Werror`؛ **یک اخطار = بسته‌بندی رد می‌شود**؛
  - **کل سوئیت تست** اجرا می‌شود؛ از درخت قرمز بسته ساخته نمی‌شود؛
  - **خودِ بسته** (نه درخت بیلد) روی یک پورت یدکی اجرا می‌شود و `/menu`
    باید واقعاً جواب فارسی بدهد، وگرنه آرشیوی ساخته نمی‌شود.
- `fix(tools)`: سرورِ دود-آزمایی نشت می‌کرد و پورت را نگه می‌داشت — دو باگ
  پشت سر هم: `$!` به‌جای خود بازی، **زیرپوسته** را می‌گرفت (با `exec` حل
  شد)، و بازی SIGTERM را برای خاموشی تمیز می‌گیرد پس یک `kill` ساده کافی
  نبود (حالا انتظار و سپس SIGKILL و راستی‌آزمایی).
- `fix(app)`: **`kimia_world --help`** اصلاً پیاده نشده بود — تلاش می‌کرد
  سرور را بالا بیاورد و با `web server failed to start` می‌مرد. حالا
  راهنمای کامل چاپ می‌کند.
- `fix(app)`: **گزینهٔ ناشناخته دیگر بی‌صدا نادیده گرفته نمی‌شود.** غلط
  تایپی مثل `--porrt 9000` قبلاً بی‌صدا رد می‌شد و بازی روی پورت اشتباه
  بالا می‌آمد؛ حالا پیام خطا + راهنما و **کد خروج ۲**. (قاعدهٔ مشخصات:
  «هیچ no-op خاموش».)
- `test`: تستی که تضمین می‌کند هر فایل پروفایلِ **ارسالی در بسته** واقعاً
  لود می‌شود و ترتیب/تعداد منو را عوض نمی‌کند (فایلی که بی‌صدا رد شود یعنی
  بازی‌ای که بازیکن نمی‌تواند انتخاب کند)، و این‌که کامنت‌های انتهایی
  `golf.kimiaprofile` (دستور باد) هیچ مقداری را تغییر نمی‌دهند.
- `docs`: `Documentation/Release.md` (سند کامل بسته‌بندی)، بخش «نسخهٔ
  آفلاین» در README، و ROADMAP.

**آزمایش واقعی** — بسته ساخته شد، در پوشهٔ دیگری باز شد و مثل یک بازیکن
اجرا شد: `sh play.sh` → منو در مرورگر → «دنیای جدید» → «گلف کیمیا» → سوراخ
و توپ از کاتالوگ → PLAY → شوت واقعی با نگه‌داشتن «شوت» (قدرت ۶۹٪، ضربه ثبت
شد) → «ذخیره» → فایل در `worlds/my_world.kimia` (با خط `# profile wind`) →
بازی بسته و دوباره باز شد → «باز کردن دنیا» → همان زمین برگشت.

---

## نسخهٔ 0.3.0 — باد و رکورد بهترین دور (مرحلهٔ ۲۰٫۵ ب-۲)

| | |
| --- | --- |
| **نسخهٔ موتور** | `0.3.0` (یک مرحله = فرعی +۱) |
| **تاریخ امروز** | یکشنبه ۱۵ شهریور ۱۴۰۵ — 2026-09-06 |
| **زمان توسعهٔ این نسخه** | حدود ۴ ساعت (۰۱:۲۳ تا ۰۵:۳۰ به وقت تهران) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۳۵ ساعت |
| **تست‌ها** | 222/222 (۱۴ تست جدید) — صفر اخطار در هر ۴ گیت بیلد + `ctest` سبز |

**چرا** — آخرین بند باز مرحلهٔ ۲۰٫۵ پیش از انتشار نسخهٔ آفلاین گلف: باد
(که از مرحلهٔ ۲۴ جلو افتاد چون گلف بدون آن کامل نیست) و ذخیرهٔ بهترین دور
در فایل دنیا. هم‌زمان مستندات با کد واقعی هم‌گام شد.

**چه تغییر کرد**

- `feat(physics)`: **باد** — `Wind` و `makeWind(speed, direction)` در
  `Engine/Physics`. باد یک **شتاب افقی** (m/s²) است که مثل گرانش ولی از
  پهلو روی کرهٔ دینامیک اعمال می‌شود و با `SphereBody::windFactor` مقیاس
  می‌گیرد. سه قانون که آن را «بازی» نگه می‌دارند:
  - توپِ **ایستاده** باد نمی‌خورد (اصطکاک نگهش داشته) — وگرنه یک نسیم
    توپ را تا ابد در زمین می‌راند؛ ملاک، سرعت افقیِ ورودی به گام است.
  - روی زمین فقط `kWindGroundFactor = 0.35` باد می‌رسد (چمن بقیه را
    می‌گیرد) و این فشار به `kWindGroundGrip = 0.5` از بودجهٔ اصطکاک سطح
    محدود می‌شود، پس اصطکاک همیشه در نهایت برنده است و توپ می‌ایستد.
  - در هوا باد کامل اثر می‌کند و **دترمینیستیک** است: همان شوت در همان
    باد، در ۶۰ و ۱۲۰ هرتز بیت-یکسان فرود می‌آید.
- `feat(profile)`: کلید `wind <شتاب> <جهت>` در `*.kimiaprofile` (کلمپ
  ۰…۲۰، خط ناقص کامل رد می‌شود، `%.6f` و بایت-یکسان). همهٔ بازی‌های
  موجود **آرام** می‌مانند (`wind 0 0`) — هیچ بازی‌ای تغییر رفتار نداد.
  `Profiles/golf.kimiaprofile` خط بادی را به‌صورت کامنت آماده دارد.
- `feat(world)`: باد از پروفایلِ **خودِ دنیا** به فیزیک می‌رود (دنیای
  ذخیره‌شده در همان بادی می‌ماند که ساخته شد)، `windHudText()` جهت باد را
  **نسبت به نشانه** روی HUD می‌نویسد (`WIND 3 <-` / `->` / `^` / `v`) و
  `| wind N` به خط آمار اضافه شد.
- `feat(world)`: **رکورد بهترین دور** — `WorldData::bestRound`. دوری که
  همهٔ سوراخ‌هایش زده شده رکورد می‌گیرد؛ فقط دور **کمتر** آن را می‌شکند.
  در پایان دور روی HUD `NEW BEST n` یا `BEST n` می‌آید و `| best n` در خط
  آمار. رکورد به‌صورت `# best n` در فایل `.kimia` ذخیره می‌شود — **فقط
  وقتی دوری تمام شده باشد**، پس فایل دنیاهای بازی‌نشده بایت-به-بایت مثل
  قبل است و فایل‌های قدیمی بدون رکورد بار می‌شوند.
- `test`: ۱۴ تست جدید با مقادیر واقعی — انحراف بستهٔ باد
  (`x_N = a·dt²·N(N+1)/2`)، عدم جابه‌جایی توپ ایستاده زیر شدیدترین باد
  مجاز در ۱۰ ثانیه، `windFactor` (۰ و ۰٫۵)، دترمینیسم ۶۰ در برابر ۱۲۰
  هرتز، **دنیای آرام بیت-یکسان با نبودِ باد** (گارد رگرسیون)، خم شدن پات
  در بودجهٔ اصطکاک، رکورد (ثبت/عدم شکست با دور بدتر/شکست با دور بهتر/
  ذخیره و بارگذاری)، و HUD باد نسبت به نشانه.
- `docs`: `TechnicalSpec.md` با کد واقعی هم‌گام شد (۲۰۸→۲۲۲ تست، ۲۲ صفحهٔ
  ماشین حالت، خطوط و حجم واقعی ماژول‌ها و باینری‌ها، ردیف `kimia_profile`،
  مسیرهای `/sound` و `/sfx/<name>`، محدودیت فارسیِ HUD)، مسیر غلط
  `build/kimia_assets_cli` → `build/bin/…` در README و Assets.md، و
  `Physics.md`/`Profile.md`/`World.md`/`Golf.md` با بخش باد و رکورد.

---

## نسخهٔ 0.2.1 — بیلد روی Termux/Clang، اسکریپت نصب یک‌دستوره، هشدار شاخهٔ `main`

| | |
| --- | --- |
| **نسخهٔ موتور** | `0.2.1` (رفع باگ بیلد + ابزار کوچک = وصله +۱) |
| **تاریخ امروز** | یکشنبه ۱۵ شهریور ۱۴۰۵ — 2026-09-06 |
| **زمان توسعهٔ این نسخه** | حدود ۴۵ دقیقه (۲۳:۴۵ تا ۰۰:۳۰ به وقت تهران) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۳۱ ساعت |
| **تست‌ها** | 208/208 — صفر اخطار با GCC 12 و **Clang 21** (`-Werror` روشن و خاموش)، و در بیلد headless |

**چرا** — کاربر موتور را روی گوشی پاک کرد و دوباره کلون کرد: `git clone`
شاخهٔ `main` را می‌آورد که فقط اسکلت `Engine/Core` است؛ بیلد ۱۰۰٪ شد ولی
`build/bin/kimia_world` و `kimia_tests` کامل وجود نداشتند. موتور روی شاخهٔ
`arena/01a07125-ai-codespace` است. هم‌زمان اولین بیلد واقعی با Clang (همان
کامپایلر Termux) دو اخطار پیدا کرد که GCC نمی‌داد.

**چه تغییر کرد**

- `fix(web)`: `jsonEscape` — تبدیل ضمنی `char → unsigned char` در حلقهٔ
  range-for (`-Wsign-conversion` در Clang خطا بود)؛ حالا cast صریح.
- `build`: اخطار `-Wtautological-compare` کد فروشندهٔ `stb_vorbis.c` فقط
  برای همان هدف و فقط در Clang خاموش شد (کد upstream دست‌نخورده)؛
  `kimia_renderer` با `${CMAKE_DL_LIBS}` لینک می‌شود (`dlopen` روی glibc
  قدیمی)؛ بدون `CMAKE_BUILD_TYPE` بیلد **Release** است (رسترایزر نرم‌افزاری
  روی گوشی چند برابر سریع‌تر)؛ configure کامپایلر و build type را چاپ می‌کند.
- `Tools/termux_build.sh` (خواستهٔ بند ۶ HANDOFF): تشخیص شاخهٔ موتور و
  رفتن به آن اگر روی `main` باشی، `pkg install clang cmake ninja`، تعمیر
  `cmake` خراب، configure/build با `-DKIMIA_WERROR=ON`، شمارش اخطار، اجرای
  تست‌ها، چاپ دستور بعدی؛ `--run`، `--clean`، `--port=N`. روی یک کلون تازهٔ
  `main` آزمایش شد: خودش به شاخهٔ موتور رفت، ساخت، 208/208.
- README (بخش «نصب روی گوشی — یک دستور» با `git clone --branch …`)،
  Platform.md (نکات Termux/Clang)، HANDOFF (قاعدهٔ شاخه + Clang).

---

## نسخهٔ 0.2.0 — HUD روی فریم، صدا و دوربین پشت توپ (مرحلهٔ ۲۰٫۵ ب-۱)

| | |
| --- | --- |
| **نسخهٔ موتور** | `0.2.0` (یک مرحله = فرعی +۱) |
| **تاریخ امروز** | شنبه ۱۴ شهریور ۱۴۰۵ — 2026-09-05 |
| **زمان توسعهٔ این نسخه** | حدود ۱ ساعت و ۱۵ دقیقه (۲۲:۵۰ تا ۰۰:۰۵ به وقت تهران) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۳۰ ساعت و ۱۵ دقیقه |
| **تست‌ها** | 208/208 — بدون اخطار با `-Werror` روشن و خاموش، و در بیلد headless |

**چه اضافه شد** — سه قابلیت عمومی موتور که گلف اولین مصرف‌کننده‌شان است و
فوتبال (امتیاز تیمی، تایمر، صدای شوت/گل، دوربین سوم‌شخص) همان‌ها را
برمی‌دارد:

- **فونت بیت‌مپ داخل موتور** (`Engine/Graphics/BitmapFont.h`، `kimia::font`):
  ۹۵ گلیف ۵×۷ ASCII، جعبهٔ توخالی برای گلیف ناشناخته، `drawText/fillRect/drawBar`
  روی `Image` یک/سه/چهارکاناله — HUD در مسیر GL و نرم‌افزاری یکسان کشیده
  می‌شود. `Renderer::captureImage` برای کشیدن HUD روی فریم GL پیش از PNG.
- **HUD روی فریم:** `WorldEditor::hudLines()` (`HOLE i/n  PAR p`،
  `STROKE s  TOTAL t`، `IN! s STROKES`، `ROUND OVER T (PAR P) n UNDER|EVEN|n OVER`
  + `CARD …`؛ در حالت دروازه `SCORE n` / `GOAL!`) و `hudPower()` (نوار
  `POWER` فقط هنگام شارژ). `kimia_world` آن‌ها را بالا-چپ و پایین-وسط فریم
  می‌کشد.
- **رویدادهای بازی → صدا:** `WorldEditor::drainEvents()` با
  `GameEvent::{Shot, Kick, Holed, Goal, RoundOver}` (یک‌بارمصرف)؛ صداهای
  رویه‌ای `AudioBuffer::tone/thock/concat` + `encodeWAV()` (بایت‌به‌بایت برابر
  `writeWAV`)؛ WebViewer: `registerSound/playSound/soundSequence`، مسیرهای
  `GET /sound` (`"<seq> <name>"`) و `GET /sfx/<name>` (`audio/wav`)، پخش در
  صفحهٔ وب با کش و «مسلح‌شدن» با اولین لمس. بدون هیچ وابستگی و فایل asset.
- **دوربین پشت توپ:** `chaseCameraActive()` در حالت شوت؛ دوربین مداری با نرخ
  `6/s` پشت توپ و در جهت نشانه می‌چرخد، درگ نگاه‌کردن و `q/e` می‌مانند.
- ۱۰ تست جدید: ۴ فونت (جدول گلیف، متریک‌ها، پیکسل‌های دقیق و برش، آلفا و
  نوار)، ۲ صدا (بایت‌های WAV = فایل، قطعی‌بودن/پوش صداها)، ۱ وب (توالی صدا
  و `/sfx`)، ۳ دنیا (HUD در طول یک دور + رویدادها، واژه‌های OVER/EVEN،
  حالت دونده Kick/Goal). خط‌های HUD عمداً ASCII هستند (فونت ۵×۷ فارسی
  ندارد)؛ عنوان‌ها و منوها همچنان فارسی در صفحهٔ وب‌اند.

---

## نسخهٔ 0.1.0 — زمین گلف چندسوراخه، پار و جدول ضربه‌ها (مرحلهٔ ۲۰٫۵ الف) + شماره‌گذاری معنایی

| | |
| --- | --- |
| **نسخهٔ موتور** | `0.1.0` (اولین نسخه با شماره‌گذاری معنایی؛ یک مرحله = فرعی +۱) |
| **تاریخ امروز** | شنبه ۱۴ شهریور ۱۴۰۵ — 2026-09-05 |
| **زمان توسعهٔ این نسخه** | حدود ۲ ساعت و ۳۰ دقیقه (۱۹:۰۰ تا ۲۱:۵۵ به وقت تهران، شامل دو بار بازسازی محیط بیلد) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۲۹ ساعت |
| **تست‌ها** | 198/198 — بدون اخطار با `-Werror` روشن و خاموش، و در بیلد headless |

**شماره‌گذاری** — به خواست کاربر، نسخه دیگر تاریخ نیست: `اصلی.فرعی.وصله`
با اندازهٔ تغییر (کوچک/باگ `+0.0.1`، مرحله/بزرگ `+0.1.0`). `Version.h`
(`kEngineVersionMajor/Minor/Patch`)، `CMakeLists.txt`، تست
`engine_version_is_semantic_and_written_down` (تازه‌ترین بخش دفترچه باید
همین نسخه باشد)، `--version` = `KIMIA 0.1.0`.

**چه اضافه شد** — گلف از «یک سوراخ» به «یک دور» رسید؛ همین سازوکار بعداً
«مسابقه/پایان بازی/جدول امتیاز» فوتبال است:

- کلید پروفایل `par <n>` (۱…۲۰، پیش‌فرض ۳؛ فایل‌های قدیمی بدون این خط همان ۳).
- زمین چندسوراخه: سوراخ‌ها به ترتیب شمارهٔ نام (`Hole_1` اول) بازی می‌شوند؛
  فقط سوراخِ نوبت توپ را می‌گیرد؛ سوراخ بعدی از همان‌جا زده می‌شود (مینی‌گلف)؛
  عنوان «سوراخ ۲ از ۳ — …» و «رفت تو سوراخ! ضربه‌ها: N — بعدی: سوراخ M».
- جدول ضربه‌ها و صفحهٔ «پایان دور! ۱ ۰ ۲ | جمع ۳ | پار ۹ | ۶ زیر پار» با
  «دور جدید» (توپ به تی، سوراخ ۱، جدول خالی) و «منو»؛ «توپ از نو» = دور از اول.
- خط آمار: `| hole i/n | total T | par P`؛ پرچم قرمز روی سوراخ نوبت در فریم.
- ۲ تست جدید (زمین سه‌سوراخهٔ خارج از ترتیب، رد شدن از روی سوراخ غیرنوبت،
  واژه‌های «بالای پار/برابر پار»، زمین بدون سوراخ) + پین‌های `par` در
  پروفایل و فایل دنیا.

---

## نسخهٔ 1405.06.14.3 — گلف روی KIMIA World (مرحلهٔ ۲۰)

| | |
| --- | --- |
| **نسخهٔ موتور** | `1405.06.14.3` (سومین نسخهٔ همان روز) |
| **تاریخ امروز** | شنبه ۱۴ شهریور ۱۴۰۵ — 2026-09-05 |
| **زمان توسعهٔ این نسخه** | حدود ۱ ساعت (۱۷:۵۵ تا ۱۸:۵۵ به وقت تهران) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۲۶ ساعت |
| **تست‌ها** | 196/196 — بدون اخطار با `-Werror` روشن و خاموش، و در بیلد headless |

**چه اضافه شد** — بازی اول («گلف کیمیا») دیگر جزیره نیست؛ روی همان ادیتور و
همان پروفایل‌ها ساخته می‌شود و هر آجرش برای فوتبال می‌ماند:

- دو کلید جدید پروفایل: `mode kick|shot` (دونده یا نشانه/شارژ/رها) و
  `scoring gate|hole` (دروازه یا سوراخ). پیش‌فرض‌ها همان رفتار قبلی‌اند؛
  فایل‌های قدیمی بدون تغییر.
- پروفایل داخلی پنجم و اولِ منو: **گلف کیمیا** (`Profiles/golf.kimiaprofile`:
  زمین ۱۰×۲۴، توپ دقیق، `kick 2.5 13.5 0` = اعداد پرتاب گلف مرجع).
- جسم «سوراخ» در کاتالوگ (برای همهٔ بازی‌ها): دیسک تیره، بدون برخورد، قانون
  کاپ مرجع (شعاع ۰٫۲۸ متر و کندتر از ۵ m/s).
- حالت شوت در PLAY: پد «← نشانه / نشانه → / شوت (نگه دار)»، قدرت ۰→۱ با دور
  زدن، شوت بعدی از محل توقف توپ، ضربه‌شمار (`| stroke N | power P%`)، توپ در
  حرکت زده نمی‌شود، «رفت تو سوراخ! ضربه‌ها: N» و بازگشت به شروع با ضربهٔ صفر؛
  زنجیرهٔ نشانه روی زمین که با شارژ بلند می‌شود.
- `kimia_world`: کلید ۹ برای گزینهٔ نهم، Space در حالت شوت نگه‌داشتنی است
  (در فوتبال همچنان پرش).
- ۶ تست عددی گلف روی World (از جمله شوت مرجع قدرت ۰٫۶۱ از ۱۴ متر که واقعاً
  به سوراخ می‌رود، و «۸ m/s رد می‌شود / ۳ m/s می‌افتد / ۱ m/s در ۰٫۱۲۷ متر
  می‌ایستد»).

---

## نسخهٔ 1405.06.14.2 — نام رسمی بازی‌ها و نقشهٔ چهار بازی

| | |
| --- | --- |
| **نسخهٔ موتور** | `1405.06.14.2` (دومین نسخهٔ همان روز) |
| **تاریخ امروز** | شنبه ۱۴ شهریور ۱۴۰۵ — 2026-09-05 |
| **زمان توسعهٔ این نسخه** | حدود ۴۵ دقیقه (۱۷:۱۰ تا ۱۷:۵۵ به وقت تهران) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۲۵ ساعت |
| **تست‌ها** | 190/190 — بدون اخطار با `-Werror` روشن و خاموش، و در بیلد headless |

**چه تغییر کرد**

- نام رسمی بازی‌ها در پروفایل‌های داخلی، فایل‌های `Profiles/`، منوی
  «کدام بازی؟»، تست‌ها و مستندات:
  **«فوتبال خیابونی ایران: کوی ابوذر»** · **«زمین چمن: کوی ابوذر»** ·
  **«مسابقه واقعی: بتل گراند»** (شناسه‌های `street` / `grass` /
  `battleground` عوض نشدند؛ فایل‌های دنیای قبلی همان‌طور باز می‌شوند).
- نقشهٔ چهار بازی و ترتیب ساخت در README و ROADMAP:
  ۱) **گلف** — آزمایش و یادگیری موتور؛ ۲) فوتبال خیابونی — اولین بازی جدی؛
  ۳) زمین چمن — جذب بقیهٔ فوتبالی‌ها؛ ۴) بتل گراند — زمان‌بر.
- **سیاست انتشار:** هر بازی اول کامل می‌شود، سپس آنلاین منتشر می‌شود؛ تا آن
  زمان نسخهٔ آفلاین در دسترس است. ساخته‌های هر بازی در موتور می‌مانند و
  بازی بعدی از همان‌ها استفاده می‌کند.
- سامانهٔ نسخه‌گذاری (`Version.h` + CMake + تست + این دفترچه) و
  `kimia_world --version` — در همین روز اضافه شد.

---

## نسخهٔ 1405.06.14 — «پروفایل بازی» (مرحلهٔ ۱۹)

| | |
| --- | --- |
| **نسخهٔ موتور** | `1405.06.14` |
| **تاریخ امروز** | شنبه ۱۴ شهریور ۱۴۰۵ — 2026-09-05 |
| **زمان توسعهٔ این نسخه** | حدود ۳ ساعت (۱۴:۱۰ تا ۱۷:۱۰ به وقت تهران؛ از ادغام شاخه تا این دفترچه) |
| **زمان توسعهٔ تجمعی موتور** | حدود ۲۴ ساعت در دو روز (۱۳ و ۱۴ شهریور) |
| **تست‌ها** | 190/190 — بدون اخطار با `-Werror` روشن و خاموش، و در بیلد headless |
| **کامیت‌ها** | ۷ کامیت در این نسخه (`12614ee` … این دفترچه)؛ ۸۲ کامیت از ابتدای مخزن |

**چه اضافه شد**

- **یک موتور، سه بازی.** `Engine/Profile`: `GameProfile` + فرمت متنی
  `*.kimiaprofile` (لود مقاوم، ذخیره→بارگذاری→ذخیره بایت‌به‌بایت یکسان).
  چهار پروفایل داخلی: فوتبال خیابونی ایران، زمین چمن، بتل گراند، زمین آزاد؛
  سه فایل قابل‌ویرایش در `Profiles/` که بدون یک خط C++ بازی را عوض می‌کنند
  یا بازی پنجمی به منو اضافه می‌کنند.
- ادیتور: «دنیای جدید → **کدام بازی؟**»؛ اندازهٔ زمین، محیط، توپ، سرعت و
  پرش بازیکن و فرمول شوت از پروفایل؛ مرزهای بازیکن/توپ/جعبه/شبح از اندازهٔ
  زمین؛ محیط جدید «آسفالت (خیابان)»؛ بازی‌های تک‌توپ سؤال «دقیق یا فانتزی»
  را نمی‌پرسند.
- هر دنیای `.kimia` پروفایلش را با خود حمل می‌کند (`# profile …`)؛
  فایل‌های قدیمی بدون تغییر باز می‌شوند (زمین آزاد، اندازه از `Ground`).
- `Engine/Core/TextFormat.h` (کمک‌تابع‌های مشترک قالب متنی)؛ وابستگی
  `kimia_world → kimia_golf` قطع شد.
- مستندات: `Documentation/Profile.md`، نقشهٔ راه مراحل ۱۹ تا ۲۹ (طرح سه
  بازی)، اصلاح شمارش‌های کهنه، بخش کنترل‌گر کاراکتر در `Physics.md`.
- همین دفترچه و `Version.h` (نسخهٔ موتور در کد، `kimia_world --version`).

**تصمیم‌های طراحی ثبت‌شده در این نسخه**

- ترکیب تیم‌ها: خیابونی **۵ در برابر ۵** · زمین چمن **۱۱ در برابر ۱۱** ·
  بتل گراند فعلاً **۴ در برابر ۴**.
- بتل گراند: تقریباً مثل PUBG، **بدون pay-to-win**؛ دو حالت — **آرنا**
  (دو تیم ۴ نفره) و **بتل گراند** (۱۲ تیم ۴ نفره در نقشهٔ خاورمیانه، ۸ تیم
  ۴ نفره در نقشهٔ جنگل).

---

## نسخهٔ 1405.06.13 — بازسازی کامل موتور (مرحله‌های ۱ تا ۱۸)

| | |
| --- | --- |
| **نسخهٔ موتور** | `1405.06.13` (در CMake آن زمان `0.1.0` نوشته شده بود) |
| **تاریخ** | جمعه ۱۳ شهریور ۱۴۰۵ تا بامداد شنبه ۱۴ شهریور — 2026-09-04 تا 2026-09-05 |
| **زمان توسعه** | حدود ۲۱ ساعت (۰۶:۱۳ روز ۱۳ شهریور تا ۰۳:۰۳ بامداد ۱۴ شهریور؛ `21de190` تا `c4f243a`) |
| **تست‌ها** | 175/175 |
| **کامیت‌ها** | ۷۳ کامیت (`21de190` … `c4f243a`) |

**چه ساخته شد (به ترتیب زمانی کامیت‌ها)**

- هسته (Types، Log، Profiler، FixedTimeStep) · ریاضیات (Vec، Mat4، Quat،
  Camera، تصویر متعامد، دوربین مداری) · دارایی‌ها (مش‌های اولیه؛
  OBJ+MTL، FBX با متریال/تکسچر جاسازی‌شده، PNG/JPG، WAV/MP3/OGG/FLAC؛
  ابزار `kimia_assets`) · Scene و فرمت متنی SceneIO v1.
- فیزیک: گام ثابت، کره در برابر صفحه/جعبه، اصطکاک و آرام‌گیری، جعبه‌های
  دینامیک با حل‌گر ضربه‌ای تکرارشونده، خروج امن از همپوشانی، کنترل‌گر
  کاراکتر kinematic (گرانش، پرش √(2gh)، برخورد-و-لغزش).
- رندر: GL/EGL با dlopen + سایهٔ PCF، و رسترایزر نرم‌افزاری کامل (برش
  صفحهٔ نزدیک) برای گوشی بدون GPU؛ ردیاب پرتو آفلاین (PBR، GI، BVH،
  خروجی دترمینیستیک) و ابزار `kimia_raytrace`.
- پلتفرم و اپ: ورودی، پنجرهٔ SDL2 اختیاری، WebViewer (فریم PNG + منوی
  JSON روی localhost) برای Termux.
- بازی مرجع **گلف** و سپس **KIMIA World**: ویرایشگر گزینه‌محور فارسی —
  ساخت دنیا، افزودن/مدیریت/بازرسی اجسام، مدل از فایل، محیط، PLAY با
  دروازه و گل، ذخیره/بارگذاری، دوربین مداری.

---

## نسخهٔ 1405.06.05 — آغاز مخزن

| | |
| --- | --- |
| **تاریخ** | پنج‌شنبه ۵ شهریور ۱۴۰۵ — 2026-08-27 |
| **کامیت** | `da76355 Initial commit` |

مخزن ساخته شد؛ کد موتور از ۱۳ شهریور آمد.
