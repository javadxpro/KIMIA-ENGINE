// DdaPolicy.cpp — header-only implementation lives in DdaPolicy.h.
// This file exists so the linker finds a symbol even if -O3 inlines
// everything.
#include <kimia/DdaPolicy.h>
namespace kimia::street {
// intentionally empty
}
