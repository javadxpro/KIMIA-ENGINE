// DensityMass.cpp — header-only helpers; provide non-template depots here if
// any are added later. Functions currently inlined in DensityMass.h.
#include <kimia/DensityMass.h>
namespace kimia::street {
// intentionally empty; all bodies live in the header.
}
